# 太虚幻境 v1.6 — 技术白皮书

## 目录

1. [项目概述](#1-项目概述)
2. [系统架构](#2-系统架构)
3. [技术栈与依赖](#3-技术栈与依赖)
4. [核心引擎设计](#4-核心引擎设计)
5. [LLM 集成层](#5-llm-集成层)
6. [叙事生成管线](#6-叙事生成管线)
7. [NPC 智能体系统](#7-npc-智能体系统)
8. [记忆与知识系统](#8-记忆与知识系统)
9. [世界模拟系统](#9-世界模拟系统)
10. [蝴蝶效应与因果引擎](#10-蝴蝶效应与因果引擎)
11. [高级叙事机制](#11-高级叙事机制)
12. [数据持久化与存储](#12-数据持久化与存储)
13. [前端架构](#13-前端架构)
14. [安全机制](#14-安全机制)
15. [插件系统](#15-插件系统)
16. [小说角色扮演模式](#16-小说角色扮演模式)
17. [学术理论引用](#17-学术理论引用)
18. [性能与扩展性](#18-性能与扩展性)
19. [v1.5 第二期 — 动机/立场/传承/归档](#19-v15-第二期--动机立场传承归档)
20. [总结](#20-总结)
21. [v1.6 优化工程 — P1~P4 全阶段](#21-v16-优化工程--p1p4-全阶段)

---

## 1. 项目概述

**太虚幻境**（Chronoverse）是一个基于大语言模型（LLM）的虚拟世界人生模拟器。玩家通过自然语言与一个动态演化的虚构世界交互，系统实时生成叙事文本、驱动 NPC 自主行为、管理世界经济与社会关系，并维持长期的故事一致性。

### 1.1 核心特性

- **AI 驱动叙事**：所有故事内容由 LLM 实时生成，每次游戏体验独一无二
- **多智能体协调**：玩家、NPC、世界各由独立 AI 代理管理，形成多智能体协作系统
- **闭环学习**：叙事审查器从历史中提取经验教训，反馈到后续生成
- **分层记忆**：短期记忆 + 长期摘要 + 长期身份语义核心，三重记忆架构
- **蝴蝶效应**：玩家行为通过因果网络产生非线性世界影响
- **开放世界生成**：支持 8 种世界类型（历史/奇幻/科幻/末日/武侠/仙侠/现代/自定义）
- **SillyTavern 生态兼容**：角色卡、世界书双向导入导出
- **小说角色扮演**：导入既有小说，选择角色与章节进入，注入原著记忆/关系图谱，支持 dormant NPC 概率登场与蝴蝶效应偏离度追踪（v12+）
- **NPC 行动裁决层**：ActionArbiter 过滤 AI 空想行为，骰子引擎概率判定，视野隔离硬执行，NPC 分层智能（LOD），职业日程模板，休眠 NPC 唤醒推演（v1.2）
- **世界自主运行**：AutoRun 引擎让世界自我演化 N 天，NPC 自主行动，自动生成章节体叙事（v1.2）
- **LLM 预算控制**：BudgetGuard 三层保护（每日USD/每回合上限/熔断），超支自动降级，防失控（v1.3）
- **NPC 性格演化**：创伤事件触发性格转折，LLM 生成新性格描述，7 天冷却期，持久化轨迹（v1.3）
- **NPC 私密档案**：LLM 生成 3-5 条私密事实（秘密/过往/好恶/未言之心愿），普通模式由玩家开关控制，小说模式默认启用（v1.3）
- **因果链可视化**：多维度重要性计算（蝴蝶效应/规则效果/NPC 参与度/性格转折/伏笔），Cytoscape.js 图谱展示，全模式通用（v1.3）
- **统一记忆框架**：5 个常驻 md 文件（world_brief/npc_dossiers/player_profile/active_threads/meta_memory），双触发更新（每 8 回合增量 + 跨日全量整合），解决 Memory Curator 摘要未注入 prompt 的断层（v1.5 P1）
- **自适应 Ebbinghaus 遗忘曲线**：半衰期随游戏天数 `sqrt(day/10 + 1)` 自适应，重要性影响衰减，修仙场景万年级记忆不失效（v1.5 P1）
- **世界时钟与事件系统**：世界时钟随玩家回合推进，跨日生成 0-3 个玩家事件（NPC 主动找你）+ 独立 10% 概率世界事件，弹窗式事件推送，闭关/隐居降低打扰概率（v1.5 P1）
- **NPC 动机引擎**：6 类动机触发器（survival/social/career/exploration/legacy/transcendence），每日 ×0.7 衰减，强度 <20 移除，纯规则不调 LLM（v1.5 P2）
- **立场名誉系统**：7 级立场（仁善→唯我），玩家行为关键词识别→立场差异反应，名誉→初次见面加成，纯规则（v1.5 P2）
- **血缘传承**：NPC 自然死亡触发继承（长子→长女→配偶→徒弟→家族成员），reputation ×0.8 + faction_standing 全量 + stats ×0.6（上限 20）（v1.5 P2）
- **事件历史归档**：终态事件从 events 移到 archive（上限 2000 条），多维筛选查询 API（v1.5 P2）
- **容器化部署**：Dockerfile + docker-compose，支持服务器托管/多实例管理，普通玩家仍用 `启动.bat`（v1.4）
- **存档版本迁移**：链式迁移框架（`@register` 装饰器注册），每步自动备份，支持断点续传（v1.4）
- **Prompt 注入防护**：玩家输入 fence 包裹 + 控制字符过滤 + 长度截断（v1.4）
- **GameEngine Mixin 拆分**：拆分为 MetaFacadeMixin + NpcFacadeMixin，主类减少 502 行（v1.4）
- **金手指叙事规范**：开启金手指时禁止「系统提示」「好感度+5」等游戏化文本，所有超凡元素必须用小说笔法融入叙事；关闭时给出自然否定叙事（v1.5.1）
- **自主运行时间预估**：根据推演天数动态估算等待时间（7天内5-7分钟/30天内10-20分钟），确认弹窗和进度界面双重提示（v1.5.1）
- **存档加载世界观恢复**：加载存档/槽位时从 world_def 恢复世界观简介并显示在最前，解决"历史情节在但世界观丢失"问题（v1.5.1）

### 1.2 版本演进

| 版本 | 核心能力 |
|------|---------|
| v1-v8 | 基础叙事、NPC 系统、经济、存档 |
| v1.2 | NPC 行动裁决层（ActionArbiter/DiceEngine/PerceptionScope/DailyRoutine/DormantWake/GoalEvaluator/NpcLOD/NpcReaction/AutoRun） |
| v9 | 安全中间件、访问令牌、EventBus |
| v10 | 闭环学习、多智能体叙事、分层记忆、蝴蝶审批门 |
| v10+ | NPC 技能自学、自主记忆管理、MCP 工具协议 |
| v10+++ | 后台 NPC 生成、SillyTavern 兼容 |
| v11 | 访问令牌同步、撤销操作、多客户端隔离 |
| v12 | GraphRAG 时序关系、角色动态状态（CHIRON）、场景自适应检索 |
| v12+ | 小说角色扮演：剧情介绍生成、dormant NPC 概率登场、蝴蝶效应偏离度追踪 |
| v1.3 | LLM 预算控制（BudgetGuard）、NPC 性格演化轨迹、NPC 私密档案、因果链可视化 |
| v1.4 | 认证漏洞修复（DNS rebinding）、8 层健康检查、双缓存治理、GameEngine Mixin 拆分、json_object 原生模式、统一叙事持久化、容器化部署、存档版本迁移框架、Prompt 注入防护 |
| v1.5 P1 | 统一记忆框架（5 个 md 文件）、Ebbinghaus 自适应遗忘曲线、世界时钟与事件系统、每世界独立图像文件夹、聊天记录完整保存 |
| v1.5 P2 | NPC 动机引擎（6 类）、立场名誉系统（7 级）、血缘传承、事件历史归档（2000 条） |
| v1.5.1 | 金手指叙事规范（禁止游戏化文本）、自主运行时间预估提示、加载存档恢复世界观简介 |

---

## 2. 系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                    太虚幻境 - 系统架构                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐  │
│  │  REST API     │  │  WebSocket   │  │  静态文件服务          │  │
│  │  (11 Router)  │  │  (流式输出)   │  │  (Alpine.js SPA)     │  │
│  └──────┬───────┘  └──────┬───────┘  └──────────────────────┘  │
│         │                 │                                      │
│         └────────┬────────┘                                      │
│                  │                                               │
│  ┌───────────────▼────────────────────────────────────────────┐  │
│  │                   GameEngine（中央协调器）                    │  │
│  │         依赖注入容器 / 50+ 子系统引用 / 线程安全锁              │  │
│  └───────────────────────────┬────────────────────────────────┘  │
│                              │                                   │
│  ┌───────────────────────────▼────────────────────────────────┐  │
│  │                    TurnProcessorV2                          │  │
│  │            16 步回合处理管线 / 意图分类 / 规则引擎              │  │
│  └───────────────────────────┬────────────────────────────────┘  │
│                              │                                   │
│  ┌──────────┬────────────────┼────────────────┬───────────────┐  │
│  │          │                │                │               │  │
│  ▼          ▼                ▼                ▼               ▼  │
│ ┌────┐  ┌───────┐  ┌──────────────┐  ┌──────────┐  ┌────────┐ │
│ │ LLM│  │ AI    │  │ 叙事引擎      │  │ 世界模拟  │  │ 记忆   │ │
│ │ 路由│  │ 代理  │  │              │  │          │  │ 系统   │ │
│ └────┘  └───────┘  └──────────────┘  └──────────┘  └────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 模块分层

| 层级 | 职责 | 模块数量 |
|------|------|---------|
| **接入层** | HTTP/WebSocket/静态文件 | 11 路由文件 |
| **协调层** | GameEngine + TurnProcessorV2 + WorldManager | 3 |
| **AI 代理层** | PlayerAgent + NPCAgent + WorldAgent | 3 |
| **叙事引擎** | NarrativeEngine + NarrativeStyle + PromptAssembler | 4 |
| **记忆系统** | ChromaDB + BM25 + GraphRAG + HybridRetriever | 8 |
| **世界模拟** | Age/Economy/Quest/Faction/Death/Level/Weather | 15+ |
| **NPC 系统** | 自治/感知/反思/技能/预测/生命演化 | 10 |
| **持久化** | ChromaDB/SQLite/MetaDB/SaveManager | 5 |
| **基础设施** | EventBus/Registry/Plugin/LLM/Security | 8 |

---

## 3. 技术栈与依赖

### 3.1 后端技术栈

| 技术 | 用途 | 版本要求 |
|------|------|---------|
| Python 3.11 | 主语言 | 捆绑便携版 |
| FastAPI | Web 框架 | ≥0.110.0 |
| Uvicorn | ASGI 服务器 | ≥0.29.0 |
| OpenAI SDK | LLM API 调用 | ≥1.30.0 |
| ChromaDB | 向量数据库 | ≥0.5.0 |
| Pydantic | 数据验证 | ≥2.0.0 |
| PyCryptodome | 配置加密 | ≥3.20.0 |
| AIOSQLite | 异步数据库 | ≥0.20.0 |
| NetworkX | 知识图谱 | ≥3.0 |
| Tenacity | 重试机制 | ≥8.2.0 |
| WebSockets | 实时流式输出 | ≥12.0 |

### 3.2 前端技术栈

| 技术 | 用途 |
|------|------|
| Alpine.js | 响应式 UI 框架 |
| Cytoscape.js | 影响力网络/知识图谱可视化 |
| WebSocket | LLM 流式输出接收 |
| 原生 CSS | 羊皮纸风格主题 |

### 3.3 外部 API 依赖

| API | 用途 | 模型 |
|-----|------|------|
| DeepSeek API | 主要/对话/廉价 LLM | deepseek-v4-flash |
| SiliconFlow API | 向量嵌入 + 文生图 | BAAI/bge-m3, Kwai-Kolors/Kolors |
| Agnes AI API | 备用 LLM + 图片生成 | agnes-2.0-flash, agnes-image-2.1-flash |

---

## 4. 核心引擎设计

### 4.1 GameEngine — 中央协调器

`GameEngine`（`game_engine.py`，2100+ 行）是整个系统的中枢，采用**依赖注入容器 + Mixin 拆分**模式管理 50+ 个子系统实例。[v1.4] 通过 `MetaFacadeMixin` + `NpcFacadeMixin` 拆分，主类减少 502 行。

**核心职责**：
- 子系统生命周期管理（创建、初始化、关闭）
- LLM 实例管理与路由
- 游戏创建/加载/保存协调
- 插件系统挂载
- 线程安全的游戏锁（`_game_lock`）

**关键属性**：

```python
class GameEngine(MetaFacadeMixin, NpcFacadeMixin):
    # LLM 路由
    llm: LLMRouter          # 三级 LLM 路由器
    embedding_fn             # SiliconFlow bge-m3 嵌入函数

    # AI 代理
    player_agent: PlayerAgent
    npc_agent: NPCAgent
    world_agent: WorldAgent

    # 叙事系统
    narrative: NarrativeEngine
    multi_agent_narrative: MultiAgentNarrativeEngine

    # 记忆系统
    memory: MemoryStore      # ChromaDB
    hybrid_retriever         # BM25 + 向量 + GraphRAG
    graph_rag: GraphRAG
    memory_curator: MemoryCurator
    memory_brief: MemoryBriefManager  # [v1.5 P1] 5 个 md 常驻层

    # 世界系统
    economy: EconomySystem
    age_system: AgeSystem
    death_system: DeathSystem
    world_task_board: WorldTaskBoard
    quest_system: QuestSystem
    world_tick: WorldTick            # [v1.5 P1] 世界时钟与事件
    player_event_bus: PlayerEventBus # [v1.5 P1] 玩家事件队列
    world_event_bus: WorldEventBus   # [v1.5 P1] 世界事件队列

    # NPC 系统
    npc_perception: NPCPerceptionSystem
    npc_reflection: NPCReflection
    npc_procedural_memory: NPCProceduralMemory
    npc_skill_library: NPCSkillLibrary
    social_network: SocialNetwork

    # 高级机制
    butterfly: ButterflyEffect
    foreshadow_lifecycle: ForeshadowLifecycle
    continuity_auditor: ContinuityAuditor
    autonomous_memory: AutonomousMemoryManager

    # [v1.3] 高级机制
    causal_graph: CausalGraph      # 因果链可视化图
    is_novel_roleplay: bool        # 是否处于小说扮演模式

    # [v1.5 P2] 第二期子系统
    motivation_engine: MotivationEngine    # 6 类动机触发器
    family_registry: FamilyRegistry        # 家族注册表（内存）
    inheritance_service: InheritanceService  # 血缘传承服务
    # alignment_system 为纯函数模块，不需要实例引用
```

### 4.2 ServiceRegistry — 服务注册表

`ServiceRegistry`（`registry.py`，433 行）作为工厂函数，统一创建所有子系统并注入依赖关系：

```python
@dataclass
class ServiceRegistry:
    # 50+ 可选子系统字段
    # create_services() 工厂函数：
    #   1. 创建三个 LLM 实例（main/dialogue/cheap）
    #   2. 绑定到 PlayerAgent/NPCAgent/WorldAgent
    #   3. 创建所有子系统并交叉注入
    #   4. 加载插件
    #   5. 返回完整 ServiceRegistry 实例
```

### 4.3 TurnProcessorV2 — 16 步回合管线

`TurnProcessorV2`（`turn_processor_v2.py`，2380 行）是实际生产的回合处理器，替代了 v9 的 12 步管线。

**完整处理流程**：

```
Step 1:  意图分类（IntentClassifier）→ 11 种意图类型
Step 2:  世界规则评估（WorldRulesEngine）→ 确定性规则判定
Step 3:  动作验证（Cheap LLM 快速拒绝不可能动作）
              ↳ [v1.5 P2] 校验前仅识别玩家行为类型（detect_player_action）
Step 4:  规则应用（伤害/声望/标签/状态效果）
              ↳ [v1.5 P2] 校验通过后应用立场反应（apply_player_action_to_npcs）
Step 5:  固定提示词组装（空间一致性/叙事风格/社交网络/规则提示/教训/工作记忆/伏笔/角色状态/场景检测）
              ↳ [v1.5 P1] 注入 MemoryBrief 常驻层（5 个 md 文件）
Step 6:  叙事生成（根据意图选择模板/轻量LLM/完整LLM/多智能体）
Step 7:  选项刷新（检测通用选项并重新生成上下文相关选项）
Step 8:  骰子判定（技能检定/随机事件）
Step 9:  记忆更新（ChromaDB 存储）
              ↳ [v1.5 P1] Ebbinghaus 自适应遗忘曲线 + 访问频次跟踪
Step 10: 社交网络更新（关系变化/信息传播）
Step 11: 时间感知（NarrativeTimekeeper 解析叙事中的时间跳跃）
              ↳ [v1.5 P2] 序数日检测（"第一日...第十日..."触发跨日）
Step 12: 蝴蝶效应（异步评估 + 下回合应用）
Step 13: 死亡检查（DeathSystem）
              ↳ [v1.5 P2] 自然死亡触发血缘传承
Step 14: 异步子系统（RAG 存储/GraphRAG/角色状态分析/身份审计/经验/历史记录）
Step 14.7: [v12+] 小说模式 foreshadow 失效检查（偏离度超阈值时）
Step 14.8: [v1.3] NPC 性格演化检测（创伤事件正则匹配，LLM 生成新性格，7 天冷却）
Step 14.9: [v1.3] 因果链节点记录（多维度重要性计算，超阈值写入 causal_graph）
Step 15: NPC 子系统（程序性记忆/技能学习/任务板/任务系统）
Step 16: 异步维护（叙事审查/记忆策展/伏笔追踪/连续性审计/自主记忆/自动存档）
              ↳ [v1.5 P1] 每 8 回合触发 MemoryBrief 增量更新
              ↳ [v1.5 P1] 跨日时触发 MemoryBrief 全量整合（consolidate_on_sleep）
```

**生成策略选择**：

| 意图类型 | 策略 | 说明 |
|---------|------|------|
| OBSERVE/REST/MANAGE | TEMPLATE | 零 LLM 消耗，模板生成 |
| EXPLORE/TRADE/TRAVEL/CRAFT | LIGHT_LLM | 2048 token 上下文，快速响应 |
| DIALOGUE/COMBAT/QUEST/CUSTOM | FULL_LLM | 完整上下文 + 两阶段生成 |
| 关键剧情（多条件启发式） | MULTI_AGENT | 三智能体协作（PlotArchitect → CharacterReviewer → DialogueWriter） |

---

## 5. LLM 集成层

### 5.1 三级 LLM 架构

系统采用三级 LLM 架构，根据任务复杂度自动路由：

```
┌─────────────────────────────────────────┐
│              LLMRouter                   │
├─────────────┬──────────────┬────────────┤
│   Main LLM  │ Dialogue LLM │  Cheap LLM │
│  (叙事生成)  │  (玩家对话)   │  (分类/评分) │
│ deepseek-v4  │ deepseek-v4   │ deepseek-v4 │
│  -flash      │  -flash       │  -flash     │
└─────────────┴──────────────┴────────────┘
         ↕                    ↕
    ┌─────────────────────────────┐
    │  RuleBasedFallbackLLM       │
    │  (兜底：60s 超时 → 硬编码)   │
    └─────────────────────────────┘
```

**路由规则**（`router.py`，478 行）：
- 对话类任务 → Dialogue LLM
- 分类/评分/简单任务 → Cheap LLM
- 叙事/JSON 生成 → Main LLM
- 含中文分类关键词的短提示 → Cheap LLM
- 任何模型失败 → 自动降级到其他可用模型

### 5.2 MimoLLM — 核心 LLM 实现

`MimoLLM`（`mimo_llm.py`，911 行）基于 OpenAI 兼容 API 的 LLM 实现：

**关键特性**：
- **[v1.4] 双缓存治理**：内部 `_cache` 字典保留为向后兼容占位，所有缓存读写优先委托给 `external_cache`（`LLMCache` 实例），由 `GameEngine` 注入；未注入时不进行任何缓存，避免与 LLMCache 不一致
- **内存缓存**：MD5 键 + 300 秒 TTL，线程安全，支持输入模式提取
- **自动扩容**：`finish_reason=length` 时自动增加 `max_tokens`（最大 32768）
- **结构化输出**：`chat_structured()` 支持 JSON Schema 参数，回退到提示词引导；[v1.4] 新增 `json_object` 原生模式（比 `json_schema` 更通用），首次调用失败自动降级
- **健壮的 JSON 解析**：多层回退 — 清理 markdown 围栏 → 移除尾逗号 → 修复未转义换行 → 提取最外层对象
- **截断检测**：括号深度计数检测不完整 JSON
- **启动预检**：初始化时验证 API 可达性
- **[Bug 修复] BaseLLM `**kwargs` 兼容**：`chat_json_from_messages` 和 `chat_structured` 接受任意额外参数（如 `narrative_hint`/`task_type`/`retries`），避免 Router 调用时报 `unexpected keyword argument`

### 5.3 结构化输出系统

`StructuredOutputManager`（`structured_output.py`，290 行）定义了 7 种 JSON Schema：

| Schema | 用途 |
|--------|------|
| NARRATIVE_SCHEMA | 叙事文本 + 状态变化 + 标签 + 效果 + 关系 |
| OPTIONS_SCHEMA | 3 个差异化玩家选项 |
| NPC_ACTION_SCHEMA | NPC 行动决策（行动/位置/意图） |
| BUTTERFLY_SCHEMA | 蝴蝶效应评估（分数/类型/后果） |
| INTENT_SCHEMA | 意图分类（类型 + 关键词 + 复杂度） |
| AUDIT_SCHEMA | 身份一致性审计结果 |
| WORLD_SCHEMA | 世界生成结构（名称/设定/势力/NPC） |

**[v1.4] json_object 原生模式**：相比 `json_schema` 模式更通用，大多数 OpenAI 兼容 API 都支持；通过模型名自动判断（DeepSeek/Qwen/GLM/Yi 默认不支持 schema），首次调用失败自动降级到 prompt 引导。

### 5.4 [v1.3] BudgetGuard — LLM 预算控制系统

`LLMRouter` 内置三层预算保护机制，防止 LLM 调用失控导致成本超支：

```
┌─────────────────────────────────────────┐
│           BudgetGuard 三层保护            │
├─────────────────────────────────────────┤
│                                         │
│  Layer 1: 每日 USD 预算                  │
│  • daily_budget_usd（0=不限制）          │
│  • 超支 → 降级到 cheap 模型              │
│                                         │
│  Layer 2: 每回合调用上限                  │
│  • per_turn_limit（0=不限制）            │
│  • per_turn_window_sec=30s 滑动窗口      │
│  • 超限 → 降级到 cheap 模型              │
│                                         │
│  Layer 3: 熔断保护                       │
│  • circuit_failure_threshold=5 次失败    │
│  • circuit_recovery_sec=30s 自动恢复     │
│  • 熔断时只走 fallback，不调用任何 LLM    │
│                                         │
└─────────────────────────────────────────┘
```

**配置**（`config.json` → `llm_budget`）：

```json
{
  "llm_budget": {
    "enabled": true,
    "daily_budget_usd": 0.0,
    "per_turn_limit": 8,
    "per_turn_window_sec": 30.0,
    "circuit_failure_threshold": 5,
    "circuit_recovery_sec": 30.0
  }
}
```

**运行时 API**（4 个端点）：
- `GET /api/llm-budget/status` — 当前预算状态（今日消费/调用次数/熔断状态）
- `GET /api/llm-budget/aggregate` — 聚合统计（任务类型分布/Token 消耗/成本估算）
- `POST /api/llm-budget/reset-daily` — 手动重置每日预算计数器
- `POST /api/llm-budget/reset-circuit` — 手动关闭熔断（紧急恢复）

**降级链路**：主 LLM → cheap LLM → RuleBasedFallbackLLM（硬编码兜底）

---

## 6. 叙事生成管线

### 6.1 NarrativeEngine — 叙事引擎

`NarrativeEngine`（`narrative_engine.py`，251 行）负责多种叙事类型的生成：

| 方法 | 输出 |
|------|------|
| `generate_daily_chapter()` | 日事件日志 → 章节体叙事 |
| `generate_scene_narrative()` | 场景叙事（地点/时间/天气/参与者） |
| `generate_dynamic_options()` | 基于场景的 LLM 选项生成 |
| `generate_morning_intro()` | 每日晨间场景描写 |
| `generate_novel_chapter()` | 完整小说章节（含属性/经济/蝴蝶效应） |
| `generate_world_evolution()` | 世界进化摘要 |

### 6.2 叙事风格系统

`NarrativeStyleManager`（`narrative_style.py`，222 行）管理 6 种预设写作风格：

| 风格 | 特征 |
|------|------|
| 章回体 | 半文半白，「话说」「且说」开头，悬念收尾 |
| 半古半文 | 文言句式与白话交融，说书人口吻 |
| 大白话 | 现代口语化，轻松幽默，短句为主 |
| 严肃文学 | 冷峻克制，细节描写，情感内敛 |
| 网文爽文 | 快节奏，奇遇连连，主角光环，升级打怪（v1.5.1 移除"系统提示频繁""数据化呈现"，改为小说笔法融入） |
| 诗化散文 | 意境优先，景物描写，富有诗意 |

**两级控制**：全局默认（config.json）> 世界覆盖。支持 LLM 辅助从上传文本提取风格。

### 6.3 [v1.5.1] 金手指叙事规范

金手指开关控制主角是否拥有超凡能力（系统/特殊能力/穿越者记忆等），但两种模式都必须保持小说质感，禁止游戏化文本格式。

**开启金手指时**（`player_agent.py` `_get_golden_finger_rule`）：

```
✓ 允许：系统/特殊能力/穿越知识/奇遇/机缘
✗ 禁止：「系统提示：开始XX任务」「叮！」「好感度+5」「经验值+100」「属性面板」
→ 所有超凡元素必须通过小说笔法融入叙事：
  • 系统（如有）通过主角内心感受、脑海中声音展现
  • 好感度变化通过 NPC 表情/动作/语气自然展现
  • 任务通过剧情线索引出，不弹任务面板
```

**关闭金手指时**：
- 明确否定主角拥有系统/特殊能力，给出自然叙事（"他并没有什么金手指"）
- 穿越者身份约束中标注"第5条金手指规则优先级高于本条"

**网文爽文风格修正**（`narrative_style.py`）：
- 移除"系统提示频繁""数据化呈现（战力值/经验值）"
- 改为"奇遇连连，主角光环"，即使是爽文也不允许游戏化文本

**穿越者身份约束**（`player_prompts.py`）：
- 金手指开启时：穿越者可利用现代知识，但不能出现游戏化面板
- 金手指关闭时：穿越者身份仅作为性格背景，无超凡能力加持

### 6.4 多智能体叙事引擎

`MultiAgentNarrativeEngine`（`multi_agent_narrative.py`，265 行）在**关键剧情时刻**启用三智能体协作：

```
PlotArchitect（剧情架构师）
    │ 生成剧情大纲：开场→发展→转折→结局
    ▼
CharacterReviewer（角色审核员）
    │ 审核：角色一致性/能力边界/关系/知识范围/存活状态
    │ 生成修改意见
    ▼
PlotArchitect._revise_outline()（最多 1 次修订）
    │
    ▼
DialogueWriter（执笔者）
    │ 基于大纲 + 角色意见撰写最终叙事
    ▼
NarrativeDraft（最终输出）
```

**触发条件**（`_is_critical_plot()` 启发式）：需同时满足 2+ 条件：
- 包含关键剧情关键词
- 蝴蝶效应分数 ≥ 5
- 伏笔召回触发
- 连续被动回合后的转折
- 多 NPC 场景

---

## 7. NPC 智能体系统

### 7.1 多层 NPC 架构

```
┌─────────────────────────────────────────┐
│           NPC 智能体系统                  │
├─────────────────────────────────────────┤
│                                         │
│  ┌───────────────────────────────────┐  │
│  │  NPCAgent（完整 AI 代理）          │  │
│  │  • BranchPlanner（树思维规划）      │  │
│  │  • ProceduralMemory（程序性记忆）   │  │
│  │  • SkillLibrary（技能库）          │  │
│  │  • CharacterStateManager（动态状态）│  │
│  │  • NPCReflection（反思）          │  │
│  └───────────────────────────────────┘  │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │  NpcAutonomous（轻量自治）         │  │
│  │  • 日常行为生成                    │  │
│  │  • 位置移动                        │  │
│  │  • 批量处理（25%概率）             │  │
│  └───────────────────────────────────┘  │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │  辅助系统                          │  │
│  │  • NPCPerception（感知区域）        │  │
│  │  • NPCRegistry（知识分级目录）      │  │
│  │  • NPCSpawner（后台生成）          │  │
│  │  • NpcLifeEvolution（年度演化）     │  │
│  │  • NPCPrediction（状态预测）       │  │
│  └───────────────────────────────────┘  │
│                                         │
└─────────────────────────────────────────┘
```

### 7.2 树思维规划器（BranchPlanner）

`BranchPlanner`（`branch_planner.py`，757 行）实现 Tree-of-Thoughts（ToT）规划：

**规划流程**：
1. **目标分解**：将长期目标分解为 4 类分支（生存/社交/职业/探索）
2. **评分剪枝**：四维评估 — 可行性(0.35) + 价值(0.30) + 风险(0.20) + 新颖性(0.15)
3. **行动生成**：基于评分最高的分支生成具体行动
4. **模拟验证**：模拟执行结果，失败则回溯
5. **MBTI 调节**：根据 NPC 人格类型调整行动优先级
6. **程序性记忆调节**：历史有效行动加分，失败行动降权

### 7.3 NPC 感知区域系统

`NPCPerceptionSystem`（`npc_perception.py`，188 行）基于距离的模拟区域：

| 区域 | 概率 | 说明 |
|------|------|------|
| 活跃区 | 100% | NPC 所在位置，完整模拟 |
| 感知区 | 50% | 相邻位置，半模拟 |
| 传闻区 | 20% | 远距离，仅传闻传播 |
| 睡眠区 | 0% | 不活跃，仅生成睡眠事件 |

### 7.4 NPC 知识分级目录

`NpcRegistry`（`npc_registry.py`，612 行）管理 NPC 的可见性层级：

```
UNKNOWN → HEARD_OF → SEEN → ACQUAINTED → FAMILIAR → INTIMATE
  未知      听说过     见过    认识        熟悉       亲密
```

支持三种可见性模式：沉浸式（仅已知信息）、半透明（部分信息）、上帝视角（全部信息）。

### 7.5 NPC 技能自学（Voyager/Hermes 式）

`NPCSkillLibrary`（`npc_skill_library.py`，338 行）从成功交互中提取可复用技能：

- LLM 从成功行动中提取技能描述
- 相似技能合并与去重
- 效果追踪与上下文感知检索
- 向量数据库存储

### 7.6 [v1.2] NPC 行动裁决层

v1.2 引入了完整的 NPC 行动裁决体系（9 个模块，3,900+ 行），确保 NPC 行为合理且有概率约束，防止 LLM 生成不合理的"空想行为"。

**ActionArbiter**（`action_arbiter.py`，747 行）— NPC 行动裁决核心：
- 过滤 AI 生成的空想行为（不可能完成的动作）
- 基于物理/社交/能力规则进行可行性判定
- 谋划行为裁决（NPC 是否真的能完成计划中的行动）

**DiceEngine**（`dice_engine.py`，359 行）— 骰子概率引擎：
- 为行动裁决提供概率判定（成功/失败/大成功/大失败）
- 技能等级修正 + 环境修正 + 随机性
- 支持 d20 机制与自定义骰子

**PerceptionScope**（`perception_scope.py`，369 行）— 视野隔离硬执行：
- 基于距离和信息层级的视野控制
- 强制执行"NPC 只知道自己能看到/听到的"
- 防止 NPC 获得不应知道的信息

**NpcLOD**（`npc_lod.py`，325 行）— NPC 分层智能（AI Level of Detail）：
- 根据与玩家距离调整 NPC AI 计算精度
- 近距离：完整 LLM 推理
- 中距离：规则驱动简化行为
- 远距离：仅位置更新

**DailyRoutine**（`daily_routine.py`，325 行）— 职业日程模板：
- 按职业生成 NPC 每日行为时序
- 提供行为合理性基准（铁匠白天打铁、小偷夜间活动）
- 使 NPC 行为有日常节奏感

**DormantWake**（`dormant_wake.py`，478 行）— 休眠 NPC 唤醒推演：
- 玩家长时间离开某区域后，该区域 NPC 进入休眠
- 玩家返回时批量推演休眠期间 NPC 的进展
- 时间跳跃后快速补全 NPC 状态

**GoalEvaluator**（`goal_evaluator.py`，641 行）— 目标判定与演化：
- NPC 长期目标的完成度评估
- 目标动态演化（放弃/升级/分裂）
- 基于成就驱动 NPC 行为方向

**NpcReaction**（`npc_reaction.py`，347 行）— NPC 即时反应引擎：
- 对玩家行为的即时情绪/态度反应
- 基于性格 + 关系 + 情境的复合反应
- 不调 LLM 的纯规则反应

**AutoRun**（`auto_run.py`，502 行）— 世界自主运行引擎：
- 让世界自我演化 N 天，NPC 自主行动
- 自动生成章节体叙事总结
- [v1.5.1] 添加推演时间预估提示（7天内5-7分钟/30天内10-20分钟）

### 7.7 [v1.3] NPC 性格演化轨迹

`personality_evolution.py` 独立性格演化引擎，负责"稳定性格"的转折（区别于 `character_state.py` 的"动态状态"如心情/压力/伤势）。

**10 类创伤事件正则模式**：

| 事件类型 | 触发示例 |
|---------|---------|
| 亲人死亡 | 妻子/丈夫/父亲/母亲...+ 死/丧/亡/逝/遇难 |
| 被背叛 | 被出卖/背信弃义/欺骗了我 |
| 被诬陷 | 蒙冤/含冤/诬告 |
| 家破人亡 | 灭门/惨遭/满门/血洗 |
| 心愿达成 | 功成名就/夙愿得偿/大仇得报 |
| 彻底失败 | 一败涂地/万劫不复/身败名裂 |
| 被救命 | 救命之恩/救了我一命 |
| 重伤残疾 | 残疾/毁容/断臂/失明 |
| 得遇机缘 | 获得/觉醒/传承 + 神功/宝物/秘籍 |
| 失去重宝 | 失去/被夺 + 神功/宝物/修为 |

**演化流程**：
1. TurnProcessor Step 14.8 按 `check_interval_turns` 周期触发
2. 正则检测本回合叙事中是否有 NPC 经历创伤事件
3. 检查 7 天冷却期（避免频繁转折）
4. LLM 生成新性格描述 + 转折叙事
5. 写入 `NPCState.personality_history`（最多 30 条）
6. 转折叙事同时写入 `narrative_history` 供后续上下文引用

**模式无关**：普通模式与小说模式都启用。

### 7.8 [v1.3] NPC 私密档案

`npc_private_facts.py` 为 NPC 生成 3-5 条私密事实，丰富角色深度。

**4 种事实类型**：

| 类型 | 说明 | 示例 |
|------|------|------|
| secret | 秘密（不能让别人知道） | 隐疾、隐藏身份、犯罪记录 |
| past | 过往（影响性格的人生经历） | 童年创伤、师承来历 |
| preference | 好恶（可被玩家利用） | 厌恶某人、酷爱某物 |
| wish | 未言之心愿（玩家可帮助实现） | 复仇、寻亲、赎罪 |

**生成策略**：
- **小说模式**：dormant NPC 登场时自动调用，从 GraphRAG 提取原著关系作为素材
- **普通模式**：由设置开关 `features.npc_private_facts.enabled` 控制，玩家首次与 NPC 对话时懒加载触发

**API 端点**：
- `GET /api/npc-private-facts/{npc_id}` — 查询 NPC 私密档案
- `POST /api/npc-private-facts/regenerate/{npc_id}` — 重新生成
- `GET /api/npc-personality-history/{npc_id}` — 查询性格演化轨迹

**前端集成**：设置面板提供「启用 NPC 私密档案（普通模式）」复选框。

### 7.9 其他 NPC / Agent 子系统

| 模块 | 行数 | 功能 |
|------|------|------|
| `brain_whispers.py` | 138 | 性格内心独白（力量型/智慧型/谨慎型三种内心声音） |
| `mbti_styles.py` | 157 | 16 种 MBTI 决策风格系统，影响 BranchPlanner 行动优先级 |
| `agent_base.py` | 273 | 统一 Agent 基类，玩家与 NPC Agent 共享的上下文/记忆/LLM 调用逻辑 |
| `option_engine.py` | 170 | 选项生成引擎（稳健/王道/骚操作三类选项模板） |
| `context_budget.py` | 228 | 场景感知上下文预算管理，动态分配 token |
| `group_chat.py` | 273 | 多 NPC 群聊/对话管理器 |
| `character_card.py` | 215 | 角色卡标准（兼容 SillyTavern V2） |
| `novel_importer.py` | 192 | 小说导入器（提取角色/世界观） |
| `world_info_importer.py` | 314 | SillyTavern World Info 导入器 |

---

## 8. 记忆与知识系统

### 8.1 三重记忆架构

```
┌─────────────────────────────────────────────┐
│              分层记忆系统                      │
├─────────────────────────────────────────────┤
│                                             │
│  短期记忆（Short-term）                      │
│  • 滚动窗口：最近 N 条叙事                    │
│  • 直接注入 LLM 上下文                        │
│  • 每 10 条触发身份整合                        │
│                                             │
│  长期摘要（Long-term Summary）               │
│  • LLM 压缩的叙事摘要                        │
│  • MemoryCurator 定期策展                     │
│  • 去重 + 归档过时记忆                         │
│                                             │
│  长期身份语义核心（Long-term Identity）        │
│  • 价值观 / 性格特征 / 习惯 / 知识            │
│  • 双过程记忆的慢整合层                        │
│  • 每 10 次记忆更新触发整合                    │
│                                             │
└─────────────────────────────────────────────┘
```

### 8.2 混合检索系统

`HybridRetriever` 整合三种检索方式：

```
查询输入
    │
    ├──→ BM25 检索（关键词匹配）
    │
    ├──→ 向量检索（语义相似度）
    │
    └──→ GraphRAG（知识图谱遍历）
            │
            ▼
    场景自适应权重调整（SceneDetector）
            │
            ▼
    合并排序 → Top-K 结果
```

**场景自适应权重**（基于 IJHCI 2025 研究）：

| 场景类型 | BM25 | 向量 | GraphRAG |
|---------|------|------|----------|
| 动作 | 0.3 | 0.3 | 0.4 |
| 社交 | 0.3 | 0.4 | 0.3 |
| 内省 | 0.5 | 0.4 | 0.1 |
| 探索 | 0.3 | 0.3 | 0.4 |
| 商业 | 0.4 | 0.4 | 0.2 |
| 学习 | 0.3 | 0.5 | 0.2 |
| 日常 | 0.5 | 0.4 | 0.1 |

### 8.3 GraphRAG 知识图谱

`GraphRAG`（`graph_rag.py`，890 行）构建实体-关系知识图谱：

**实体类型**：人物、地点、组织、物品、概念、事件

**关系类型**（v12 扩展）：

| 类型 | 说明 |
|------|------|
| social | 社交关系 |
| spatial | 空间关系（位于/相邻） |
| temporal | 时序关系（先于/同时） |
| causal | 因果关系（导致/引发） |
| possession | 持有关系 |
| membership | 归属关系 |
| influence | 影响关系 |

**时序有效性**：关系支持 `active/superseded/expired` 三态，因果链追踪。

**双层实体验证**：
1. 规则引擎扫描（500+ 中文姓氏 + 地名/物品/组织后缀）
2. LLM 二次验证（确认候选实体）

### 8.4 MemoryCurator — 记忆策展

`MemoryCurator`（`memory_curator.py`，578 行）实现周期性记忆维护：

**策展流程**（每 15 回合触发）：
1. **合并冗余**：哈希去重 + 滑动窗口文本相似度
2. **归档过时**：>100 天的叙事 + 已故 NPC 行动
3. **提取模式**：LLM 提取玩家行为模式
4. **同步传说书**：NPC 角色变更同步到 Lorebook

**关键原则**：永远不删除，只归档和整合。归档记忆可恢复。

### 8.5 自主记忆管理（MemGPT/Letta 式）

`AutonomousMemoryManager`（`autonomous_memory.py`，400 行）模拟 MemGPT 的自管理记忆：

**6 种记忆操作**：store / retrieve / summarize / discard / archive / promote

**触发条件**：
- 上下文压力（>25K token）
- 低重要性记忆过载
- 相似记忆聚集
- 过时记忆检测

### 8.6 [v1.5 P1] 统一记忆框架（MemoryBriefManager）

`memory_brief.py`（659 行）融合 Hermes 常驻摘要 + MIRIX 分层 + Generative Agents 反思，作为 LLM system prompt 的"长期记忆常驻层"。

**5 个常驻 md 文件**（存于 `saves/{world_id}/briefs/`）：

| 文件 | 字符预算 | 内容 |
|------|---------|------|
| `world_brief.md` | 1200 | 世界设定摘要（地理/势力/时代背景） |
| `npc_dossiers.md` | 1500 | 关键 NPC 档案（性格/关系/近期动向） |
| `player_profile.md` | 1000 | 玩家形象（行为模式/已做选择/偏好） |
| `active_threads.md` | 1200 | 活动剧情线（伏笔/任务/人际关系张力） |
| `meta_memory.md` | 800 | 元记忆索引（重要事件/世界观感悟） |

总和 5000-6000 字符，远小于 LLM 上下文窗口，保证常驻注入。

**双触发更新机制**：

```
触发 1: 每 8 回合增量更新
    ├─ 仅刷新 active_threads + player_profile
    └─ LLM 异步生成，不阻塞回合

触发 2: 跨日时全量整合（consolidate_on_sleep）
    ├─ 玩家"过夜"叙事时触发
    └─ 全量重写 5 个 md 文件
```

**Prompt 注入**：`get_briefs_for_prompt()` 是 brief 进入 prompt 的唯一通道，整合 5 个 md 返回 `【长期记忆常驻层 · MemoryBriefs】`。

**数据源依赖**：
- `engine.memory_curator._history_summaries`（叙事摘要）
- `engine.foreshadow_lifecycle.get_hooks_for_prompt`（伏笔）
- `engine.quest_system.get_active_quests`（任务）
- `engine.causal_graph.nodes`（因果链）
- `engine.memory.collection`（向量记忆）
- `engine.graph_rag.entities/relations`（知识图谱）

**修复断层**：v1.3 之前 Memory Curator 生成的摘要未注入 prompt，导致 LLM 长期记忆失效；v1.5 通过 MemoryBrief 统一注入通道解决。

### 8.7 [v1.5 P1] 自适应 Ebbinghaus 遗忘曲线

`hybrid_retriever.py` 的 `_rerank` 方法实现自适应时间尺度，适配修仙/长周期小说场景：

**核心公式**：

```
base_half_life = 20.0 × sqrt(current_day / 10.0 + 1.0)
half_life = max(7.0, base_half_life × (1.0 + importance / 10.0))
time_boost = max(0.01, 0.22 × 2^(-days_diff / half_life))
```

**时间尺度自适应**（避免凡人期与仙界期同衰减率）：

| 游戏天数 | base_half_life | 场景 |
|---------|---------------|------|
| day=10 | ≈28 天 | 凡人期 |
| day=100 | ≈66 天 | 筑基 |
| day=1000 | ≈200 天 | 金丹百年闭关 |
| day=10000 | ≈632 天 | 元婴千年闭关 |
| day=100000 | ≈2000 天 | 仙界万年+ |

**重要性影响**：importance=10 的记忆半衰期 ×2，importance=0 的记忆半衰期 ×1（最低 7 天保底）。

**复习效应**（last_accessed 跟踪）：
- 3 天内被检索过的记忆 `review_boost = 0.08 × (1.0 - days_since_access / 4.0)`
- 访问频次加分 `freq_boost = min(0.05, access_count × 0.01)`（上限 +0.05）
- 重要性加分 `importance × 0.18`

**验证**：古老高重要性记忆（10,000 年前 importance=9 的"筑基"）优先于近期低重要性记忆（10 年前 importance=5 的日常事件），同时期同重要性则近期优先。

---

## 9. 世界模拟系统

### 9.1 时间与年龄系统

`AgeSystem`（`age_system.py`，256 行）：

- **6 个时间段/天**：清晨/上午/中午/下午/傍晚/深夜
- **4 个季节**：春/夏/秋/冬（各 30 天循环）
- **天气系统**：季节加权随机（夏季 30% 雷暴）
- **12 个年龄里程碑**（18-60 岁）：每个触发属性变化 + 叙事
- **时间压力**：轻度(30+) / 中度(40+) / 严重(50+)

### 9.2 AMM 经济系统

`EconomySystem`（`economy.py`，460 行）实现恒定乘积做市商（x × y = k）：

**15 种商品，4 个等级**：

| 等级 | 商品 |
|------|------|
| 食物 | 大米、鱼、肉、蔬菜、茶、酒 |
| 材料 | 木材、铁、布匹、丝绸 |
| 产品 | 剑、药品、书籍 |
| 奢侈品 | 古董、灵石 |

**价格修正因子**：季节 + 事件类型（战斗/政治/自然/经济/社会/犯罪）+ 危机等级 + 随机波动

### 9.3 世界规则引擎

`WorldRulesEngine`（`world_rules.py`，372 行）实现确定性规则判定：

**设计原则**："代码处理逻辑，LLM 处理叙事"

**规则类别**：
- 暴力规则（击杀/战斗/盗窃）
- 社交规则（宴会/贿赂/慈善）
- 探索规则
- 休息规则
- 训练规则
- 交易规则

每条规则返回确定性效果（生命值/能量/声望/标签/危机等级变化），不依赖 LLM 判定。

### 9.4 世界任务板

`WorldTaskBoard`（`world_task_board.py`，488 行）实现轻量级看板系统：

- **自动生成**：从世界事件中自动创建任务
- **智能分配**：基于 NPC 角色/标签/MBTI/能量匹配
- **依赖链**：任务间支持前置依赖
- **完成效果**：任务完成时应用世界效果

### 9.5 任务链系统

`QuestChainManager`（`quest_chain.py`，586 行）支持多步骤分支任务：

- **条件系统**：字符串条件如 `"tag:穿越者"`, `"gold:50"`, `"favor:张三:50"`
- **道德追踪**：每个选择有道德对齐 + 分数
- **延迟效果**：N 天后触发的延迟后果（如 7 天后"商路恢复畅通"）

### 9.6 死亡与转世系统

`DeathSystem`（`death_system.py`，173 行）：

**死亡触发**：
1. 生命值 ≤ 0（受伤致死）
2. 能量 ≤ 0 且生命 < 20（过劳致死）
3. 年龄 ≥ max_age（默认 80）+ 10% 概率/回合
4. 自愿自杀

**死后选项**：重载存档 / 转世重生（继承记忆/标签）/ 新世界

### 9.7 百世书系统

`HundredLifeBook`（`hundred_life_book.py`，394 行）：

- 100 页转世记录
- 因果业力追踪
- 世界观特定的业力观察者（凡人→锦衣卫，修真→天道意志，魔法→古神，科幻→时空管理局）
- 跨世知识继承
- 成就提取

### 9.8 宿命遗憾系统

`DestinyRegret`（`destiny_regret.py`，128 行）：

- 错过机会的不可逆追踪（如拒绝师父传功、放弃宝藏）
- 玩家选择产生宿命标记，影响后续剧情
- 遗憾累积到阈值触发叙事回调（如旧友重提往事）

### 9.9 玩家回忆录

`PlayerMemoir`（`player_memoir.py`，156 行）：

- 玩家人生关键节点自动记录
- 章节体回忆录生成
- 与百世书联动，跨世继承精华

### 9.10 群聊系统

`GroupChat`（`group_chat.py`，273 行）：

- 多 NPC 同场景对话管理
- NPC 之间的对话关系与插话逻辑
- 群聊中 NPC 性格差异表现

### 9.11 历史知识库 RAG

`RagHistorical`（`rag_historical.py`，180 行）：

- 明朝等朝代的历史知识库
- 补充 LLM 历史知识盲区
- 按世界类型自动加载对应历史知识

### 9.12 [v1.5 P1] 世界时钟与事件系统

`WorldTick`（`world_tick.py`，321 行）实现世界时钟随玩家回合推进，跨日时生成事件。

**跨日流程**：

```
玩家输入 → 叙事时间跳跃 → 跨日触发 → WorldTick.tick()
                                              │
                                              ├─ 计算今日打扰概率
                                              │   ├─ 基础概率 0.55
                                              │   ├─ 闭关 ×0.04（≈0.02）
                                              │   ├─ 隐居 ×0.18（≈0.1）
                                              │   ├─ 状态系数（重伤 0.4 / 昏迷 0.1 / 中毒 0.5）
                                              │   └─ 位置系数（荒野 0.3 ~ 皇宫 1.4）
                                              │   上限 0.9
                                              │
                                              ├─ roll 事件数（60%/30%/10% → 1/2/3 个）
                                              │
                                              ├─ _make_player_event(npc, day)
                                              │   ├─ [v1.5 P2] 先 decay 现有动机
                                              │   ├─ [v1.5 P2] roll 新动机（如无活跃）
                                              │   ├─ [v1.5 P2] 按动机类型选模板
                                              │   └─ 回退到好感度模板
                                              │
                                              └─ 10% 独立概率生成世界事件
                                                  └─ 8 种类型：war/beast_tide/discovery/...
```

**节奏控制**：
- 连续 3 天有事件后强制 1 天平静
- 全部用模板生成不调 LLM
- 玩家事件次日过期，世界事件保留 7 天

**前端集成**：
- 右侧 UI 双事件面板（玩家事件 / 世界事件）
- 聊天中弹窗式事件通知（不打断叙事流）
- 世界事件注入主叙事区（`[世界公告]` 前缀）

### 9.13 [v1.5 P1] 事件总线（PlayerEventBus / WorldEventBus）

`world_event.py`（281 行）实现统一事件结构：

```python
@dataclass
class GameEvent:
    event_id: str         # evt_xxx
    category: str         # player | world
    priority: str         # urgent | important | normal
    event_type: str       # visit/war/discovery/...
    title: str
    summary: str
    source_npc: str       # 触发 NPC（玩家事件）
    trigger_day: int
    expire_day: int
    status: str           # pending/accepted/rejected/expired/missed
    payload: dict
```

**两个总线**：
- `PlayerEventBus` — 管理 NPC 主动找玩家的事件队列
- `WorldEventBus` — 管理世界级公告（战争/兽潮/发现/势力变更/旱灾/瘟疫/宝物/叛乱）

**持久化**：原子写入 `saves/{world_id}/state/events.json`

**模板库**：
- `PLAYER_EVENT_TEMPLATES`：按好感分桶（high_favor/neutral/enemy/low_favor）
- `WORLD_EVENT_TEMPLATES`：8 种世界事件模板

---

## 10. 蝴蝶效应与因果引擎

### 10.1 蝴蝶效应系统

`ButterflyEffect`（`butterfly_effect.py`，373 行）评估玩家行为的世界影响：

**评估流程**：
1. 记录玩家行动（位置/标签/结果预览）
2. LLM 评估影响分数（0-10）+ 类型（个人/地方/区域/全球）+ 后果
3. 分数 ≥ 5 时生成宏观事件（500-1000 字描述）
4. 高分行动进入审批门（阈值 7.0）

**审批门机制**：
- 低影响 → 自动执行
- 高影响 → 生成预览 → 排队审批 → 玩家批准/拒绝/修改

### 10.2 伏笔生命周期

`ForeshadowLifecycle`（`foreshadow_lifecycle.py`，349 行）管理叙事伏笔：

**生命周期**：插入 → 提及 → 解决/推迟

**双存储**：ChromaDB（向量搜索）+ 本地 hooks 字典（状态管理）

**检测机制**：
- 过时检测：30 天未解决的伏笔
- 突发检测：8-12 阈值，防止伏笔过载

### 10.3 连续性审计

`ContinuityAuditor`（`continuity_auditor.py`，361 行）每 N 回合异步执行五维审计：

| 维度 | 说明 |
|------|------|
| character_identity | 角色身份一致性 |
| resource_continuity | 资源连续性 |
| timeline_consistency | 时间线一致性 |
| personality_drift | 人格漂移检测 |
| foreshadow_payoff | 伏笔回收检查 |

### 10.4 影响力网络

`InfluenceNetwork`（`influence_network.py`，251 行）基于图的影响力传播：

- BFS 传播，`衰减因子 × (边权重/100)` 每跳衰减，最大 3 跳
- Cytoscape.js 可视化（前端）
- 影响级联追踪

### 10.5 [v1.3] 因果链可视化

`causal_graph.py` 记录玩家决策点 → 后果链的有向图，用于 debug 可视化与回溯分析。

**节点结构**（CausalNode）：

| 字段 | 说明 |
|------|------|
| turn_id | 回合号 |
| day | 游戏内天数 |
| player_input_excerpt | 玩家输入摘要（前80字） |
| narrative_excerpt | AI 叙事摘要（前120字） |
| importance | 重要性分数（0-15） |
| triggered_events | 触发的事件类型列表 |
| effects_summary | 后果摘要（200字内） |
| parent_turn_ids | 上游依赖回合（因果链） |
| novel_divergence | 小说模式蝴蝶效应偏离度 |
| timestamp | 实际时间戳 |

**多维度重要性计算**（`compute_importance`）：

| 维度 | 权重 | 上限 |
|------|------|------|
| 蝴蝶效应分（impact_score，0-10） | × 0.5 | 5 |
| 小说偏离度（novel_divergence，0-100） | × 0.05 | 5 |
| 规则效果数 | × 0.5/条 | 3 |
| 涉及 NPC 数 | × 0.4/个 | 3 |
| 触发性格转折 | +3 | - |
| 触发伏笔 | +2 | - |
| 玩家输入长度 >50/100 字 | +1/+2 | - |
| 叙事长度 >500/1000 字 | +1/+2 | - |

**阈值过滤**：默认 `min_importance=6.0`，低于阈值的回合不记录，避免噪音。可在 `config.json` → `features.causal_graph.min_importance_to_record` 调整。

**容量管理**：默认 `max_nodes=500`，FIFO 淘汰最旧节点。

**可视化**（Cytoscape.js）：
- 节点颜色：红（≥10 重大）/ 橙（≥8 重要）/ 蓝（普通）
- 节点大小：20 + importance × 2（上限 50）
- 边：基于 `parent_turn_ids` 的有向因果链

**API 端点**（5 个）：
- `GET /api/causal-graph` — 完图（Cytoscape elements 格式），支持 `?limit=N` 仅返回最近 N 个
- `GET /api/causal-graph/recent?n=20` — 最近 N 个节点（倒序）
- `GET /api/causal-graph/turn/{turn_id}` — 指定回合节点详情
- `POST /api/causal-graph/clear` — 清空（debug 用）
- `GET /api/causal-graph/stats` — 统计信息（总数/最高/平均/按事件/按天）

**持久化**：随存档保存到 `game_state.json` 的 `causal_graph` 字段，加载时通过 `CausalGraph.from_dict` 恢复。

**前端入口**：游戏顶部 🔮 按钮 → 因果链可视化 Modal，支持节点点击查看详情、刷新、清空。

---

## 11. 高级叙事机制

### 11.1 意图分类器

`IntentClassifier`（`intent_classifier.py`，313 行）实现规则驱动的意图分类：

**11 种意图类型**：观察/探索/对话/交易/战斗/休息/旅行/管理/制造/任务/自定义

**策略映射**：意图类型 → 生成策略（模板/轻量LLM/完整LLM）

### 11.2 上下文工程

`ContextEngine`（`context_engine.py`，283 行）实现注意力预算管理：

**9 层上下文优先级**：

| 层级 | 优先级 | 可压缩 | 最小 token |
|------|--------|--------|-----------|
| system | 1 | 否 | - |
| world | 2 | 否 | - |
| character | 3 | 否 | - |
| player_input | 2 | 否 | - |
| active_foreshadows | 3 | 否 | - |
| foreshadow | 5 | 是 | 200 |
| recent | 5 | 是 | 500 |
| rag | 6 | 是 | 200 |
| summary | 7 | 是 | 100 |

**分配算法**：两轮分配 — 保护层优先，可压缩层按优先级排序后头尾截断。

**Prompt Cache**：MD5 哈希稳定前缀（system + world + character），跟踪命中/未命中。

### 11.3 叙事审查器

`NarrativeReviewer`（`narrative_reviewer.py`，418 行）实现闭环学习：

**审查流程**（每 10 回合）：
1. LLM 审查近期叙事
2. 提取经验教训（3 类：玩家偏好/叙事质量/世界一致性）
3. 教训注入后续生成提示词
4. 质量指标 EMA 追踪（一致性/参与度/节奏/角色深度）

### 11.4 场景检测器

`SceneDetector`（`narrative_scene_detector.py`，200 行）基于 IJHCI 2025 研究：

检测 7 种场景类型，动态调整 GraphRAG 检索权重（动作/探索场景启用 GraphRAG，内省场景关闭）。

### 11.5 叙事时间感知

`NarrativeTimekeeper`（`narrative_timekeeper.py`，329 行）从叙事文本中解析时间表达式：

- 中文数字解析
- 模糊时间消解
- 年份跨越检测
- 严格模式：所有跳跃需显式"后/过后/以后/之后"后缀
- 单次最大跳跃 365 天
- **[v1.5 P2 Bug 修复] 序数日检测**：新增 `PATTERN_ORDINAL_DAY` 正则，识别叙事中"第一日...第十日..."这类序数日表达
  - 至少出现 2 次序数日才触发（避免"第三日的清晨"单次提及误判）
  - 取所有提及中的最大值作为跳跃天数
  - 支持中文数字（一/十/三十）和阿拉伯数字
  - 与"X天后"等常规模式共存时取较大值

**序数日检测示例**：

| 叙事文本 | 解析结果 |
|---------|---------|
| `"第三日的清晨"` | days=0（单次不触发） |
| `"第一日...第十日"` | days=10 |
| `"第一日...第五日...第十日...第三十日"` | days=30 |
| `"第一日...第三日...三日后"` | days=3（取较大值） |

---

## 12. 数据持久化与存储

### 12.1 存储架构

```
┌─────────────────────────────────────────┐
│              数据持久化层                  │
├─────────────────────────────────────────┤
│                                         │
│  ChromaDB（向量数据库）                   │
│  ├─ main（玩家记忆）                     │
│  ├─ npc（NPC 记忆）                      │
│  ├─ foreshadow（伏笔）                   │
│  ├─ identity（身份特征）                  │
│  ├─ novel_facts（小说事实）               │
│  ├─ player_events（玩家事件）             │
│  └─ causal（因果链）                     │
│                                         │
│  SQLite                                 │
│  ├─ WorldMetaDB（世界元数据 + 叙事历史）   │
│  └─ WorldDB（事件/行动/关系/经济日志）     │
│                                         │
│  JSON 文件                              │
│  ├─ saves/{world_id}/state/             │
│  │   ├─ game_state.json                 │
│  │   │   ├─ narrative_history           │
│  │   │   ├─ _history_summaries (Curator)│
│  │   │   ├─ causal_graph [v1.3]         │
│  │   │   ├─ timekeeper [v1.5 P1]        │
│  │   │   └─ events [v1.5 P1]            │
│  │   ├─ events.json [v1.5 P1]           │
│  │   └─ npcs/{npc_id}.json              │
│  ├─ saves/{world_id}/briefs/ [v1.5 P1]  │
│  │   ├─ world_brief.md                  │
│  │   ├─ npc_dossiers.md                 │
│  │   ├─ player_profile.md               │
│  │   ├─ active_threads.md               │
│  │   └─ meta_memory.md                  │
│  ├─ saves/{world_id}/manifest.json      │
│  │   └─ version [v1.4]                  │
│  ├─ saves/{world_id}/backups/ [v1.4]    │
│  │   └─ pre_migrate_{from}_{ts}/        │
│  ├─ data/novel_roleplay/ [v12+]        │
│  │   ├─ kb/ (小说知识库)                │
│  │   └─ snapshots/ (章节快照)           │
│  └─ app/config.json                     │
│                                         │
│  静态资源                                │
│  └─ app/static/images/wst/{world_id}/ [v1.5 P1] │
│      └─ 每世界独立图片文件夹              │
│                                         │
│  ZIP 导入/导出                           │
│  └─ 含 ZipSlip 防护                      │
│                                         │
└─────────────────────────────────────────┘
```

### 12.2 存档管理

`SaveManager`（`save_manager.py`，535 行）：

- **原子写入**：临时文件 + 重命名，防止写入中断损坏
- **[v1.4] 版本迁移委托**：`_migrate_save` 方法保留为代理，实际逻辑委托给 `save_migration.py` 框架
- **时间线槽位**：支持分支存档（Git 风格并行时间线）
- **ZIP 导入/导出**：含 ZipSlip 路径遍历防护
- **[v1.5 P1] 世界删除时清理图片**：删除存档时 `shutil.rmtree` 同步删除 `static/images/wst/{world_id}/` 目录
- **[v1.5.1] 加载存档恢复世界观简介**：`/api/load` 和 `/api/slot/load` 接口新增返回 `world_intro` 字段，从 `engine.world_def` 提取世界观简介（含回退拼接逻辑）；前端 `loadGame`/`loadSlotGame` 在 `restoreHistory` 之前先显示世界观简介，解决"历史情节在但世界观丢失"问题

### 12.3 [v1.4] 存档版本迁移框架

`save_migration.py`（219 行）实现链式存档迁移：

**注册表模式**：

```python
@register("0.1.0", "0.2.0")
def _migrate_0_1_to_0_2(world_dir: Path, save_manager) -> None:
    """补 role/role_history/relation_history 字段"""
    ...
```

**核心特性**：
- **链式迁移**：`find_chain(from, to)` 自动找迁移路径（防御性上限 100 步）
- **自动备份**：迁移前备份 `world_dir` 到 `backups/pre_migrate_{from}_{timestamp}/`
- **断点续传**：单步失败抛异常，`manifest.version` 保持上一步值，下次可继续
- **版本比较工具**：`parse_version("0.2.0")` → `(0, 2, 0)`，避免字符串比较陷阱

**当前版本**：`CURRENT_VERSION = "0.2.0"`

**已注册迁移**：
- `0.1.0 → 0.2.0`：补 `role`/`role_history`/`relation_history` 字段

### 12.4 [v1.5 P1] 每世界独立图像文件夹

`VisualEngine.set_world_id()` 实现图片按世界隔离：

```python
def set_world_id(self, world_id: str):
    self.current_world_id = world_id or ""
    if world_id:
        self.output_dir = self.base_output_dir / world_id  # static/images/wst/{world_id}/
    else:
        self.output_dir = self.base_output_dir  # 向后兼容
    self.output_dir.mkdir(parents=True, exist_ok=True)
```

**关键设计**：
- 新图片保存到 `static/images/wst/{world_id}/`，URL 含 world_id 子目录
- 旧图片（wst/ 根目录）仍可通过 image_url 访问，向后兼容
- 取消 50 张图片上限（存档体积由 game_state.json 原子写入保障）
- 世界删除时 `SaveManager` 自动清理对应图片文件夹

**集成点**：GameEngine 在 `create_new_game` / `load_game` / `_load_game_state` 时调用 `set_world_id`。

### 12.5 配置加密

所有 API Key 存储时自动加密（`pycryptodome`），`config.json` 中以 `enc:` 前缀存储密文。

---

## 13. 前端架构

### 13.1 单页应用

`index.html`（1436 行）使用 Alpine.js 实现响应式 SPA：

**主要界面**：
- 创建新世界（类型选择 → AI 世界观生成 → 金手指开关）
- 现有小说角色扮演
- 加载存档

**游戏界面**：
- 叙事面板（可调宽度）
- NPC 面板（详情/编辑/预测）
- 影响力网络图（Cytoscape.js）
- 因果链可视化（Cytoscape.js）[v1.3]
- 世界地图
- 知识图谱可视化
- 新闻/事件流
- **玩家事件面板** [v1.5 P1] — 右侧 UI 显示 NPC 主动找你的事件
- **世界事件面板** [v1.5 P1] — 右侧 UI 显示世界级公告
- **弹窗式事件通知** [v1.5 P1] — 聊天中不打断叙事流
- 传说书查看器
- 死亡屏幕
- 等级/技能树
- 任务显示
- 百世书
- MCP 工具面板
- 小说章节查看器
- AI 上下文调试面板
- LLM 预算状态面板 [v1.3]

### 13.2 WebSocket 流式输出

实时 LLM 输出通过 WebSocket 推送：

```
客户端 → ws://host/ws/{client_id}?token=...
服务端 → stream_token → stream_end → result
```

支持 30 秒心跳检测，asyncio.Queue 异步桥接。

### 13.3 主题系统

羊皮纸风格（parchment）：
- 深色背景 `#0a0a0f`
- 暖色文字 `#e0d5c1`
- 强调色 `#c9a96e`
- 面板背景 `#111120`

---

## 14. 安全机制

### 14.1 访问令牌

- 启动时自动生成 `secrets.token_urlsafe(32)`
- 存储在 `.access_token` 文件
- 敏感 API 端点需 Bearer Token 认证
- 本地请求（127.0.0.1/localhost）自动放行

### 14.2 CORS 配置

- 默认仅允许本地 origin
- 配置驱动的 `allowed_origins`

### 14.3 敏感端点保护

20+ 个敏感端点需令牌认证，前缀匹配机制。

### 14.4 API Key 加密

启动时自动加密 `config.json` 中的 API Key。

### 14.5 ZipSlip 防护

ZIP 导入时验证路径遍历攻击。

### 14.6 [v1.4] DNS Rebinding 防护

修复 `/api/access-token` 认证漏洞：

- **Host header 校验**：拒绝非本机 Host（防止 DNS rebinding 攻击）
- **允许列表**：`127.0.0.1` / `localhost` / `allowed_origins` 配置项
- **绕过场景**：本地请求仍可无 token 访问，但外部 DNS rebinding 攻击被拦截

### 14.7 [v1.4] Prompt 注入防护

`prompt_utils.py`（330 行）实现多层 Prompt 注入防护：

**`sanitize_player_input(text, max_len=500)`** 三步清洗：

```
玩家输入
    │
    ▼
Step 1: 控制字符过滤
    ├─ C0 控制符（除 \t \n \r）
    ├─ 零宽字符（ZWSP/ZWNJ/ZWJ/LRM/RLM）
    ├─ BOM
    └─ 行/段分隔符
    │
    ▼
Step 2: 长度截断
    └─ 超过 500 字符截断
    │
    ▼
Step 3: Fence 包裹
    ├─ <<玩家输入开始>>
    ├─ {玩家输入}
    └─ <<玩家输入结束>>
```

**设计理念**：
- Fence 包裹告知 LLM 玩家输入边界（软约束，依赖 LLM 遵守）
- 控制字符过滤防 Unicode 隐藏指令
- 统一长度截断防止超长 prompt 攻击

**集成点**：被 `engine_npc_mixin.py` 的 `npc_chat()` / `_update_npc_impressions_with_llm()` 调用，9+ 个 modules 文件集成。

### 14.8 [v1.4] 8 层健康检查

`/api/health` 端点（`config_routes.py`）返回系统健康状态：

| 检查层 | 状态字段 | 说明 |
|-------|---------|------|
| 1. engine | engine | 引擎是否初始化（含 current_world / current_day） |
| 2. llm | llm | 主力 LLM（main_llm.model_name） |
| 3. dialogue_llm | dialogue_llm | 对话模型（未配置时 status="skip"） |
| 4. embedding | embedding | 嵌入服务（model_name） |
| 5. memory | memory | ChromaDB 记忆存储（collection.count()） |
| 6. task_queue | task_queue | 任务队列（get_stats()） |
| 7. meta_db | meta_db | MetaDB 探活（list_worlds 数量） |
| 8. saves_dir | saves_dir | saves 目录检查（index.json 是否存在） |
| 9. config | config | config.json 检查 |

**整体状态**：`status` 为 "healthy" 或 "degraded"，任一关键检查 down 则降级。

**集成点**：被 `docker-compose.yml` 的 healthcheck 调用；通过 `import server` 读取 `_APP_START_TIME` 计算 uptime。

---

## 15. 插件系统

### 15.1 架构

```
EventBus（发布/订阅）
    │
    ├── plugins/achievements.py
    ├── plugins/battle_system.py
    └── plugins/weather_enhanced.py
```

**13 种预定义事件**：on_turn_start/end, on_player_input, on_npc_action, on_narrative_generated, on_world_event, on_day_change, on_economy_update, on_npc_evolved, on_save/load, on_player_death, on_level_up

### 15.2 MCP 工具协议

`MCPToolRegistry`（`mcp_tools.py`，555 行）实现 Model Context Protocol 兼容工具层：

**12 个内置工具**：
- world_task_board.list/create/assign
- butterfly.evaluate/get_pending/approve
- memory.search
- world.get_state/advance_time
- npc.list/get_info
- foreshadow.insert/list

---

## 16. 小说角色扮演模式

[v12+] 小说角色扮演模式允许玩家导入既有小说文本，选择角色与章节进入，由 AI 在原著世界观下进行自由推演。

### 16.1 导入流程

```
上传 TXT 小说
    │
    ▼
NovelKnowledgeBase 语义分块
    │ （支持百万字以上，自动章节切分）
    ▼
GraphRAG 构建知识图谱
    │ （人物/地点/组织/物品/事件实体 + 7 种关系）
    ▼
TimelineEngine 构建时间轴
    │ （按章节顺序提取关键事件）
    ▼
角色提取 → 玩家选择扮演对象
    │
    ▼
章节选择 → 深度处理该章节前内容
    │ （只处理选定章节前的内容，节省时间）
    ▼
进入游戏
```

### 16.2 双层世界观注入

`NovelRoleplayService.enter_roleplay` 注入三层内容：

| 层级 | 内容 | 标记 |
|------|------|------|
| 明面关系 | 原著已发生的人物关系 | `is_future=False` |
| 暗面关系 | 原著后期才出现的关系 | `is_future=True` |
| 未来角色 | 后期登场的人物（dormant NPC） | `is_dormant=True` |
| 未来事件 | 后期才发生的伏笔 | `novel_future=True` |

### 16.3 Dormant NPC 概率登场机制

后期 NPC 不会立即出现，而是以 dormant 状态存在，按概率登场：

| 条件 | 登场概率 |
|------|---------|
| 基础概率 | 8%/天 |
| 地点匹配 | ×3（最高 50%） |
| 最小天数 | 3 天（防止过早登场） |
| 同日去重 | 基于 `last_action_day` |

**登场时触发**：
1. 转为活跃 NPC（`is_dormant=False`）
2. 生成相遇事件叙事
3. 激活对应的未来关系（`is_future=True` → `is_future=False`）
4. [v1.3] 自动生成私密档案（从 GraphRAG 提取原著素材）

**性能控制**：dormant NPC 被排除在 NPCAgent 主循环和离线演化之外，仅由 WorldManager 每日概率检查。

### 16.4 蝴蝶效应偏离度追踪

玩家行为会累积偏离度，超过阈值（默认 30）时自动失效所有 `novel_future` 伏笔，进入自由推演：

```python
# ButterflyEffect
novel_divergence_score: float  # 累积偏离度
divergence_threshold: float = 30.0  # 失效阈值

def record_novel_divergence(action, divergence_score, ...):
    self.novel_divergence_score += divergence_score
    if self.novel_divergence_score >= self.divergence_threshold:
        # 失效所有 novel_future foreshadows
        ...
```

TurnProcessorV2 Step 14.7 每回合调用 `check_and_invalidate_future_foreshadows` 检查。

### 16.5 剧情介绍生成

进入游戏时由 LLM 生成前情提要 + 当前处境，显示在聊天页面，解决"进入后页面空白"问题。

### 16.6 离线 NPC 演化

`npc_autonomous.py` 提供规则驱动的离线演化（不调用 LLM）：
- 短期目标推进
- 位置推断（基于目标方向）
- 状态微调（能量恢复、年龄增长）
- dormant NPC 跳过

---

## 17. 学术理论引用

太虚幻境的多个子系统借鉴了前沿 AI 研究：

| 系统 | 理论来源 | 论文/项目 |
|------|---------|----------|
| NPC 反思 | Generative Agents | Stanford (Park et al., 2023) |
| 角色动态状态 | CHIRON | EMNLP 2024 |
| 自主记忆管理 | MemGPT / Letta | MemGPT (Packer et al., 2023) |
| NPC 技能自学 | Voyager / Hermes | Voyager (Wang et al., 2023) |
| NPC 程序性记忆 | Hermes Agent | Hermes Agent |
| NPC 行为规划 | Tree-of-Thoughts | ToT (Yao et al., 2023) |
| 闭环学习 | NarrativeReviewer | 自研 |
| 场景自适应检索 | IJHCI 2025 | 相关研究 |
| 多智能体叙事 | Agents' Room | Agents' Room |
| 社交网络传播 | 社会网络分析 | BFS 传播模型 |
| AMM 经济 | DeFi 恒定乘积 | Uniswap V2 |
| NPC 性格演化 | 创伤后成长理论 | 自研 [v1.3] |
| 因果链可视化 | 决策树 + 图传播 | 自研 [v1.3] |
| 小说角色扮演 | 选择你的冒险 + 蝴蝶效应 | 自研 [v12+] |
| 统一记忆框架 | Hermes 常驻摘要 + MIRIX 分层 + Generative Agents 反思 | 自研 [v1.5 P1] |
| 自适应遗忘曲线 | Ebbinghaus 遗忘曲线 + 时间尺度自适应 | 自研 [v1.5 P1] |
| NPC 动机引擎 | 马斯洛需求层次 + Self-Determination Theory | 自研 [v1.5 P2] |
| 立场名誉系统 | D&D 阵营九宫格简化 + 社会认同理论 | 自研 [v1.5 P2] |
| 血缘传承 | 家族企业继承理论 + 遗产分配 | 自研 [v1.5 P2] |

---

## 18. 性能与扩展性

### 18.1 并发模型

- **asyncio 事件循环**：主循环异步
- **asyncio.to_thread()**：阻塞 LLM 调用放入线程池
- **游戏锁**（`_game_lock`）：防止并发状态修改
- **引擎切换锁**（`_engine_switch_lock`）：防止创建/加载竞争
- **后台任务队列**：`BackgroundTaskQueue` 处理异步子系统

### 18.2 缓存策略

| 缓存层 | 机制 | TTL |
|--------|------|-----|
| LLM 响应 | MD5 键 + 语义模糊匹配 | 300s |
| 嵌入函数 | LRU 2000 条 | 持久 |
| Config | 内存缓存 | 失效时 |
| Prompt Cache | MD5 稳定前缀 | - |
| BudgetGuard 状态 [v1.3] | 内存计数器 | 每日重置/30s 熔断恢复 |

### 18.3 约束与限制

- 单 worker 模式（`workers=1`）
- 内存依赖：2GB+ 推荐
- 网络依赖：所有 LLM 生成需联网
- 便携 Python 环境：避免系统 Python 冲突

### 18.4 扩展点

- **插件系统**：EventBus + 动态加载
- **MCP 工具协议**：标准化工具接口
- **自定义叙事风格**：LLM 辅助提取
- **自定义配置文件**：多 profile 切换
- **SillyTavern 兼容**：角色卡/世界书双向导入

### 18.5 [v1.4] 容器化部署

提供 `Dockerfile`（45 行）和 `docker-compose.yml`（33 行）支持服务器托管/多实例管理。

**Dockerfile 设计**：
- 基础镜像：`python:3.11-slim`
- 分层缓存：先 `COPY requirements.txt` 再 `pip install`，最后 `COPY app`
- 系统依赖：`build-essential`（chromadb 需要）/ `sqlite3` / `tzdata` / `ca-certificates`
- 挂载点：`/app/saves` / `/app/data` / `/app/config.json`
- 环境变量：`HOST=0.0.0.0` / `PORT=8004` / `AUTO_OPEN_BROWSER=0` / `TZ=Asia/Shanghai`

**docker-compose.yml**：

```yaml
services:
  taixuhuanjing:
    image: taixuhuanjing:latest
    container_name: taixuhuanjing
    restart: unless-stopped
    ports:
      - "8004:8004"
    environment:
      HOST: 0.0.0.0
      PORT: 8004
      AUTO_OPEN_BROWSER: 0
      TZ: Asia/Shanghai
    volumes:
      - ./data/saves:/app/saves           # 存档（含每世界 ChromaDB 索引）
      - ./data/appdata:/app/data          # 全局 MetaDB
      - ./config/config.json:/app/config.json:ro  # 配置（只读挂载）
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8004/api/health', timeout=3).read()"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
```

**关键设计**：
- 普通玩家仍用 `启动.bat`，容器化仅用于服务器托管
- 环境变量 `HOST`/`PORT`/`AUTO_OPEN_BROWSER` 优先级 > `config.json` > 默认值
- 健康检查通过 `/api/health` 端点探活
- 卷挂载分离存档/MetaDB/配置，便于备份和升级

### 18.6 [v1.4] GameEngine Mixin 拆分

为降低主类体积，将 GameEngine 拆分为多个 Mixin：

| Mixin | 行数 | 职责 |
|-------|------|------|
| `MetaFacadeMixin` | 164 | 元数据/统计查询（GraphRAG 查询/v10 dashboard/蝴蝶审批） |
| `NpcFacadeMixin` | 425 | NPC 交互（聊天/印象更新/关系同步/反思触发） |
| `engine_save_mixin` | - | 存档/加载 |
| `engine_query_mixin` | - | 查询接口 |
| `engine_card_mixin` | - | 角色卡导入导出 |
| `engine_world_mixin` | - | 世界管理 |

GameEngine 通过多继承获得所有 Mixin 方法，对外接口完全兼容。主类减少 502 行。

---

## 19. v1.5 第二期 — 动机/立场/传承/归档

第二期聚焦"NPC 自主性"与"世界真实性"，4 个子系统全部采用纯规则实现，不调用 LLM，避免性能开销。

### 19.1 NPC 动机引擎（MotivationEngine）

`motivation.py`（299 行）实现 6 类动机触发器，决定 NPC"为什么来找你"。

**6 类动机**：

| 类型 | 触发条件 | 事件模板示例 |
|------|---------|------------|
| survival | NPC HP<30% 或 饥饿/中毒 | 求助/求医/避难 |
| social | 好感≥60 或 情敌/仇人出现 | 拜访/约会/告密 |
| career | 工作/修炼/任务相关 | 求教/合作/挑战 |
| exploration | 玩家在未知地点或发现宝物 | 结伴探险/分享线索 |
| legacy | NPC 年龄>60 或 无子嗣 | 托孤/传艺/寻继承人 |
| transcendence | 仅修仙/魔法世界（突破瓶颈/渡劫） | 论道/护法/索要资源 |

**强度衰减**：

```
每日衰减: intensity × 0.7
触发时强度: 50-80（按事件重要性）
移除阈值: intensity < 20
```

**增量衰减**（修复重复衰减 Bug）：
- 每个动机记录 `last_decay_day`
- 衰减时计算 `days_passed = current_day - last_decay_day`
- 仅衰减增量天数，避免每次从 `triggered_day` 算总天数导致重复衰减

**集成**：`WorldTick._make_player_event()` 跨日时先 decay 现有动机，再 roll 新动机（如无活跃），最后用动机类型选事件模板。

### 19.2 立场名誉系统（AlignmentSystem）

`alignment_system.py`（280 行）纯规则实现玩家行为→NPC 立场反应。

**7 级立场**：

```
仁善 → 刚正 → 中庸 → 无畏 → 桀骜 → 狂邪 → 唯我
  ↑                                        ↑
 最善                                    最恶
```

**6 类玩家行为识别**（关键词匹配）：

| 行为类型 | 关键词 |
|---------|-------|
| killed_npc | 杀/击杀/斩/毒杀/灭口/处死 |
| robbed | 抢/劫/盗/偷 |
| helped | 救/助/帮/护送 |
| gifted | 送/礼/赠/献/赏赐 |
| insulted | 辱/骂/嘲讽/羞辱 |
| saved | 救命/救了...一命 |

**立场差异反应**（举例：killed_npc）：

| NPC 立场 | favor_delta | trust_delta | 描述 |
|---------|------------|------------|------|
| 仁善 | -30 | -20 | "不忍杀生" |
| 刚正 | -15 | -10 | "此举有违正道" |
| 中庸 | -5 | -5 | "..." |
| 无畏 | +5 | 0 | "果决" |
| 桀骜 | +10 | +5 | "正该如此" |
| 狂邪 | +10 | +5 | "哈哈哈哈" |
| 唯我 | +15 | +5 | "弱肉强食" |

**名誉→初次见面加成**：

```
npc.personal_reputation >= 80 → +15
npc.personal_reputation >= 50 → +5
npc.personal_reputation <= -50 → -15
npc.personal_reputation <= -80 → -5（已是敌人，再降有限）
```

**立场差异→关系基础修正**：

| diff（立场索引差） | favor_modifier |
|------------------|----------------|
| 0（同立场） | +5 |
| 1 | 0 |
| 2 | -5 |
| 3 | -10 |
| 4+ | -15 |

**集成**：
- TurnProcessorV2 Step 3 校验前仅识别行为类型（`detect_player_action`）
- Step 4 校验通过后批量应用反应（`apply_player_action_to_npcs`）
- 自动跳过休眠/垂死/昏迷/失踪/已故 NPC

### 19.3 血缘传承系统（FamilyRegistry + InheritanceService）

`family.py`（303 行）实现 NPC 自然死亡时的传承机制。

**FamilyRegistry（家族注册表）**：
- `create_family(name, founding_day, head_npc_id, traditions)` → `fam_xxx` id
- `add_member` / `remove_member`（族长离开自动选举）
- `update_reputation(family_id, delta)`：家族声望调整（-100~100）
- **仅内存，重启清空**（不持久化）

**继承顺序**（6 级）：

```
1. 长子（age 最大的 children）
2. 长女（如无长子）
3. 配偶（spouse）
4. 徒弟（siblings 中 tags 含 "徒弟"）
5. 家族其他成员（family_id 相同）
6. None（无继承人，触发 legacy 动机由 WorldTick 处理）
```

**传承内容**：

| 字段 | 继承规则 |
|------|---------|
| personal_reputation | × 0.8（衰减 20%） |
| faction_standing | 全量继承（取较大值，min(heir, deceased)） |
| stats（str/int/agi/con/cha）| × 0.6，上限 20（防滚雪球） |
| alignment | 全量继承 |
| family_id | 全量继承 |

**触发条件**：仅 NPC **自然死亡**（老死/病死）触发，被杀/战死不触发传承。

**集成**：
- `npc_life_evolution.py` 检测自然死亡时调用 `_try_inherit_on_death`
- 在 `evolve_year` 末尾批量执行待传承（`process_pending_inheritance`）
- 婚姻事件自动创建家族（`_update_family_fields`）

### 19.4 事件历史归档

`world_event.py` 的 `_BaseEventBus` 新增归档系统：

**归档触发**：
- `mark(event_id, status)`：终态事件（accepted/rejected/expired/missed）从 `events` 移到 `archive`
- `expire_old(current_day)`：过期事件归档

**容量管理**：
- `ARCHIVE_MAX = 2000`：归档上限 2000 条
- 超过上限时 FIFO 淘汰最旧归档

**多维筛选查询**：

```python
list_archive(
    category: str = None,      # player | world
    event_type: str = None,    # visit/war/discovery/...
    day_start: int = None,
    day_end: int = None,
    limit: int = 100
) -> list[GameEvent]
```

**统计 API**：`archive_stats()` 返回 `{"by_type": {...}, "by_status": {...}, "total": N}`

**HTTP API**：

```
GET /api/events/history
    ?category=player
    &event_type=visit
    &day_start=10
    &day_end=50
    &limit=100

返回:
{
    "player_events": [...],
    "world_events": [...],
    "player_stats": {"by_type": {...}, "by_status": {...}, "total": N},
    "world_stats": {...},
    "filters": {...}
}
```

**限制**：`limit` 上限 500，按 `trigger_day` 倒序。

### 19.5 NPCState Schema 扩展（v1.5 P2）

`NPCState` 新增 10 个字段（全部懒加载默认值，兼容老存档）：

```python
class NPCState:
    # [v1.5 P2] 动机/立场/家族字段
    motivations: list[dict] = Field(default_factory=list)
    alignment: str = "中庸"
    faction_id: str | None = None
    personal_reputation: int = 0
    faction_standing: dict[str, int] = Field(default_factory=dict)
    family_id: str | None = None
    parents: list[str] = Field(default_factory=list)
    spouse: str | None = None
    children: list[str] = Field(default_factory=list)
    siblings: list[str] = Field(default_factory=list)
```

**Faction Schema 扩展**（向后兼容）：

```python
class Faction:
    power: int = 50
    stability: int = 50
    ideology: str = "中庸"                              # 新
    leader: str = ""                                     # 新
    members: list[str] = Field(default_factory=list)    # 新
    enemies: list[str] = Field(default_factory=list)    # 新
    allies: list[str] = Field(default_factory=list)     # 新
```

### 19.6 单元测试覆盖

`test_phase2.py` 包含 36 项单测，覆盖：

- 动机触发/衰减/模板选择
- 立场检测/反应/差异修正
- 家族创建/成员管理/传承
- 事件归档/多维筛选/统计
- NPCState 兼容性（SimpleNamespace 模拟）

加上其他测试套件，总计 **115/115 单测通过**。

---

## 20. 总结

太虚幻境 v1.5 是一个功能丰富的 AI 驱动虚拟人生模拟器，其技术架构的核心特点：

1. **模块化设计**：100+ 个 Python 模块，清晰分层，通过 ServiceRegistry 依赖注入 + GameEngine Mixin 拆分
2. **多智能体协调**：玩家/叙事/世界各由独立 AI 代理管理，支持多智能体协作叙事
3. **分层记忆系统**：短期 + 长期摘要 + 身份语义核心 + MemoryBrief 常驻层，借鉴 MemGPT/MIRIX/Hermes 架构
4. **闭环学习**：NarrativeReviewer 从历史提取经验教训，反馈到生成管线
5. **确定性 + 生成性混合**：世界规则引擎处理逻辑，LLM 处理叙事，确保一致性
6. **学术理论集成**：Generative Agents、CHIRON、Voyager、Tree-of-Thoughts 等前沿研究的工程化落地
7. **开放生态**：SillyTavern 生态兼容、MCP 工具协议、插件系统
8. **小说角色扮演**[v12+]：导入既有小说，dormant NPC 概率登场，蝴蝶效应偏离度追踪，剧情介绍自动生成
9. **LLM 预算控制**[v1.3]：BudgetGuard 三层保护（每日USD/每回合上限/熔断），超支自动降级，防失控
10. **NPC 心理深度**[v1.3]：性格演化轨迹（创伤事件触发转折）+ 私密档案（4 类事实丰富角色）
11. **因果链可视化**[v1.3]：多维度重要性计算 + Cytoscape.js 图谱，决策回溯可追溯
12. **工程化加固**[v1.4]：DNS rebinding 防护、8 层健康检查、双缓存治理、Mixin 拆分、json_object 原生模式、容器化部署、存档版本迁移框架、Prompt 注入防护
13. **长期记忆一致性**[v1.5 P1]：统一记忆框架（5 个 md 文件）解决 Memory Curator 摘要未注入 prompt 的断层；自适应 Ebbinghaus 遗忘曲线适配修仙场景万年级时间尺度
14. **世界时钟与事件系统**[v1.5 P1]：世界时钟随玩家回合推进，跨日生成 0-3 个玩家事件 + 独立 10% 概率世界事件，弹窗式推送保持沉浸
15. **NPC 自主性与世界真实性**[v1.5 P2]：6 类动机引擎 + 7 级立场名誉 + 血缘传承 + 事件历史归档，全部纯规则实现避免 LLM 开销
16. **NPC 行动裁决层**[v1.2]：ActionArbiter 过滤 AI 空想 + 骰子概率判定 + 视野隔离 + LOD 分层智能 + 职业日程 + 休眠唤醒推演，9 个模块 3,900+ 行
17. **叙事质感保障**[v1.5.1]：金手指叙事规范禁止游戏化文本、自主运行时间预估提示、存档加载恢复世界观简介

系统总量约 **40,000+ 行 Python 代码** + **6,000+ 行前端代码**（108 个 Python 模块），代表了单人 AI RPG 引擎的较高工程复杂度。

### 20.5 v1.5.1 修改清单

| 修改项 | 涉及文件 | 说明 |
|-------|---------|------|
| 金手指叙事规范 | `player_agent.py` / `narrative_style.py` / `player_prompts.py` | 开启金手指时禁止游戏化文本，所有超凡元素融入小说叙事 |
| 自主运行时间预估 | `auto_run.js` / `index.html` | 根据推演天数动态估算等待时间，确认弹窗 + 进度界面双重提示 |
| 存档加载世界观恢复 | `game_routes.py` / `game.js` / `index.html` | `/api/load` 和 `/api/slot/load` 返回 `world_intro`，加载时先显示世界观 |

### 20.1 v1.4 新增模块清单

| 模块 | 行数 | 职责 |
|------|------|------|
| `modules/save_migration.py` | 219 | 链式存档迁移框架（`@register` 装饰器注册） |
| `modules/engine_meta_mixin.py` | 164 | GameEngine 元数据/统计查询 Facade |
| `modules/engine_npc_mixin.py` | 425 | GameEngine NPC 交互 Facade |
| `Dockerfile` | 45 | 容器化镜像构建 |
| `docker-compose.yml` | 33 | 容器编排 + 健康检查 |
| 修改模块 | - | `server.py`(DNS rebinding) / `config_routes.py`(8层健康检查) / `mimo_llm.py`(双缓存) / `base_llm.py`(**kwargs) / `prompt_utils.py`(注入防护) / `save_manager.py`(迁移委托) / 9 个 modules/*.py(集成 sanitize_player_input) |

### 20.2 v1.5 第一期新增模块清单

| 模块 | 行数 | 职责 |
|------|------|------|
| `modules/memory_brief.py` | 659 | 5 个 md 常驻记忆层 + 双触发更新 |
| `modules/world_tick.py` | 321 | 世界时钟 + 跨日事件生成 |
| `modules/world_event.py` | 281 | PlayerEventBus / WorldEventBus + 归档系统 |
| 修改模块 | - | `hybrid_retriever.py`(Ebbinghaus 自适应) / `visual_engine.py`(set_world_id) / `game_engine.py`(集成新子系统) / `schemas.py`(NPCState 扩展) / `turn_processor_v2.py`(注入 MemoryBrief) / 前端 `events.js` / `index.html`(事件面板) |

### 20.3 v1.5 第二期新增模块清单

| 模块 | 行数 | 职责 |
|------|------|------|
| `modules/motivation.py` | 299 | 6 类动机触发器（survival/social/career/exploration/legacy/transcendence） |
| `modules/alignment_system.py` | 280 | 7 级立场名誉系统 + 6 类玩家行为识别 |
| `modules/family.py` | 303 | 家族注册表 + 血缘传承服务 |
| `tests/test_phase2.py` | - | 36 项单测 |
| 修改模块 | - | `schemas.py`(NPCState +10 字段/Faction +5 字段) / `npc_life_evolution.py`(婚姻/死亡集成) / `world_tick.py`(动机驱动事件) / `world_event.py`(归档系统) / `turn_processor_v2.py`(立场反应接入) / `narrative_timekeeper.py`(序数日检测) / `base_llm.py`(**kwargs Bug 修复) / `game_routes.py`(/api/events/history 端点) |

### 20.4 测试覆盖

| 测试套件 | 测试数 | 覆盖范围 |
|---------|-------|---------|
| test_phase2.py | 36 | v1.5 P2 全部子系统 |
| test_save_migration.py | 9 | 存档版本迁移框架 |
| test_prompt_injection.py | 11 | Prompt 注入防护 |
| test_memory_brief.py | 12 | 统一记忆框架 |
| test_world_tick.py | - | 世界时钟与事件 |
| test_save_manager.py | - | 存档管理 |
| test_security.py | - | 安全机制 |
| test_llm_cache.py | - | LLM 缓存 |
| test_safe_io.py | - | 安全 IO |
| **总计** | **115+** | **全部通过** |

---

## 21. v1.6 优化工程 — P1~P4 全阶段

v1.6 阶段聚焦"从功能可用到工程卓越"，分四个阶段系统化推进：功能补全（P1）、稳定性加固（P2）、验证收尾（P3）、性能与成本（P4）。本阶段新增/修改约 **9,000+ 行代码**，测试用例从 115+ 增至 **897 个**，引入 12 个新模块，零回归。

### 21.1 阶段总览

| 阶段 | 主题 | 子任务 | 核心产出 |
|------|------|--------|---------|
| **P1** | 功能补全 | P1-4/5/6/8 | 派系社区检测、CRAG+HyDE、长期记忆摘要、情感记忆系统 |
| **P2** | 稳定性 | P2-2/P2-5 | 容错原语（retry/CircuitBreaker/timeout/safe_call）、可观测性（request_id/结构化日志/health/timing） |
| **P3** | 验证收尾 | P3-A/B/C/D | 性能基准、部署准备、841 测试、16/16 E2E 验证 |
| **P4** | 性能与成本 | P4-A-1/2 | 后处理并行化（asyncio.gather）、BudgetGuard 全覆盖、LLM pricing 配置 |

### 21.2 P1 功能补全

#### 21.2.1 P1-4 派系社区检测（Louvain 算法）

`graph_rag.py` 新增 `detect_communities` + `get_faction_visualization_data` 方法，支持三种社区检测算法：

| 算法 | 复杂度 | 适用场景 |
|------|--------|---------|
| Louvain（默认） | O(n log n) | 大型图谱，模块度优化 |
| Label Propagation | O(n) | 近线性，流式场景 |
| Greedy Modularity | O(n²) | 小型图谱，精确度高 |

**前端集成**：`faction_panel.js` + Cytoscape.js 力导向布局，sidebar 🛡️ 按钮触发。修复 `lone_entities` 重复 bug。

#### 21.2.2 P1-5 CRAG 检索自评 + HyDE 查询重写

**CRAG（Corrective RAG）**：检索后自评，低质量文档降权或丢弃。
**HyDE（Hypothetical Document Embeddings）**：先用 LLM 生成假设性答案，用其向量检索相似文档，提升语义匹配召回。

debug 开关：`features.crag_hyde.enabled`。

#### 21.2.3 P1-6 长期记忆 LLM 摘要

新模块 `long_term_summary.py`（含 `MemoryAuditLog`）实现三级摘要：

| 层级 | 触发周期 | 用途 |
|------|---------|------|
| L1 日度摘要 | 每游戏日 | 当日事件压缩 |
| L2 周期摘要 | 每 7 游戏日 | 跨日事件整合 |
| L3 里程碑摘要 | 重大事件触发 | 关键节点深度摘要 |

**容错**：LLM 失败时降级为规则式关键句抽取。**容量**：审计日志上限 200 条（FIFO）。

**API**：8 个端点（摘要管理/审计日志/里程碑操作）。**前端**：`memory_panel.js` + sidebar 🧠 按钮，L1/L2/L3 颜色区分。

#### 21.2.4 P1-8 情感记忆系统

`emotional_manager.py` 为 NPC 和玩家维护 **8 维情感状态**：

```
[喜悦/愤怒/悲伤/恐惧/惊讶/厌恶/信任/期待]
```

**核心机制**：
- `EmotionEvaluator`：用 LLM 评估叙事文本情感强度（0-1）
- `NPCEmotionState`：8 维状态向量 + 峰值+平滑衰减公式（修复强情绪过度衰减 bug）
- 情感标签写入 ChromaDB 向量记忆，影响后续检索权重
- 通过 decision prompt 注入影响 NPC 对话风格
- 每回合末尾情感衰减

**阈值触发**：强情绪（>0.7）触发情感提示注入叙事 prompt。

### 21.3 P2 稳定性加固

#### 21.3.1 P2-2 容错原语

新模块 `core/resilience.py` 提供 4 个韧性原语：

| 原语 | 功能 | 参数 |
|------|------|------|
| `retry_with_backoff` | 指数退避重试 | max_retries=3, base_delay=1s, max_delay=60s |
| `CircuitBreaker` | 熔断保护 | failure_threshold=5, recovery_timeout=30s |
| `call_with_timeout` | 超时控制 | timeout_sec=30, asyncio 兼容 |
| `safe_call` | 通用异常兜底 | fallback_value, logger |

**集成点**：
- `process_player_input`：失败时返回 fallback 叙事（保证玩家体验连续性）
- `generate_return_narrative`：同上
- 关键 LLM 调用全部走 retry_with_backoff

**测试**：`test_resilience.py` 23 个测试用例覆盖所有原语。

#### 21.3.2 P2-5 可观测性

**请求追踪**（`request_id_middleware.py`）：
- 基于 `contextvars` 的 request_id 跨异步传播
- 响应头 `X-Request-ID` 返回
- 错误响应 body 含 `request_id` 字段

**结构化日志**（`RequestIdFilter`）：
- 日志格式 `[req=xxx] [module] message`
- `RotatingFileHandler`：10MB × 5 文件轮转
- 空字符串 request_id 降级为 `-`（修复边界 bug）

**健康检查增强**（`/api/health`）：
- LLM API 可达性探活（HTTP ping）
- ChromaDB 状态检查
- 9 层状态聚合（engine/llm/dialogue_llm/embedding/memory/task_queue/meta_db/saves_dir/config）

**性能指标端点**：
- `GET /api/timing` — 返回 by_category 统计 + 最近 100 条调用记录
- `POST /api/timing/clear` — 清空计时数据（需 token）
- `GET /api/performance` — 综合性能概览
- `/api/timing` 系列受 token 保护（敏感端点）

**测试**：`test_observability.py` 11 个测试用例（8 passed, 3 skipped 需真实 token）。

### 21.4 P3 验证收尾

#### 21.4.1 P3-A 性能测试

`benchmarks/bench_game.py` 实现延迟基准测试，覆盖：
- 世界生成延迟（`/api/generate-world`）
- 玩家输入延迟（`/api/input`）
- 存档/读档延迟
- NPC 群聊延迟（`/api/group-chat`）

#### 21.4.2 P3-B 部署准备

**Docker 部署链路加固**：

| 修改 | 说明 |
|------|------|
| `.dockerignore` | 保持 `.secret_key` 排除（不烤进镜像，避免密钥随镜像分发） |
| `docker-compose.yml` | 新增 `./config/.secret_key:/app/.secret_key:ro` 卷挂载，密钥运行时注入 |
| `app/config.json.example` | 新建配置模板，所有 `api_key` 用 `YOUR_*_API_KEY` 占位，复制即可用 |

**密钥处理策略变更**：原方案考虑将 `.secret_key` 烤进镜像，改用更安全的**卷挂载注入**方式。容器内密钥必须与本机加密 `config.json` 使用的密钥一致，否则解密返回空串。

**首次部署三步**（已写入 compose 注释）：
1. `mkdir config data`
2. `cp app/config.json config/config.json`（填入明文 key，首次启动自动加密）
3. `cp app/.secret_key config/.secret_key`

#### 21.4.3 P3-C 测试补全

| 测试文件 | 测试数 | 覆盖范围 |
|---------|-------|---------|
| `test_game_engine.py` | 12 类 | __init__/plugin hooks/动作分类/上下文压力/能力系统/undo_last_turn |
| `test_npc_agent.py` | - | chat_structured 配置/无效位置不应用 |
| **小计** | **+582** | 从 259 → 841 |

**Bug 修复**：`undo_last_turn` 调用不存在的 `save_state()` 方法，改为 `SaveMixin.save_game()`。

#### 21.4.4 P3-D 端到端验证

`benchmarks/e2e_verify.py` 覆盖 7 步 16 项检查：

```
Step 1: /api/health + /api/access-token
Step 2: POST /api/generate-world（生成 10 个 NPC）
Step 3: GET /api/state + /api/npcs
Step 4: POST /api/input（2 回合叙事）
Step 5: 存档/读档循环（/api/save, /api/slots, /api/slot/save, /api/slot/load）
Step 6: POST /api/group-chat（huangfuyuan + lao Zhou 多 NPC 群聊）
Step 7: WebSocket 鉴权（错误 token 403, 正确 token ping/pong）
Step 8: GET /api/timing + /api/performance
```

**结果**：16/16 PASS。

**Bug P3-D-1 修复**：`init_llm` 的 `api_key` 参数为空时未回退到 `config["llm"]["api_key"]`，导致 API-only 客户端报 `Missing credentials`。修复 `game_engine.py:1311`，新增 `main_key = api_key or llm_cfg.get("api_key", "")`。同步修改 `CreateGameRequest`/`LoadGameRequest`/`GenerateWorldRequest` 的 `api_key`/`base_url`/`model_name` 默认值为空字符串，让回退触发。

**回归测试**：`test_game_engine.py::TestInitLlmConfigFallback` 6 个用例。

**关键行为说明**：WebSocket 在 `accept()` 前调用 `close()` 返回 HTTP 403（FastAPI/Starlette 设计），而非 close code 4001；客户端应接受 403 为有效拒绝。

### 21.5 P4 性能与成本

#### 21.5.1 P4-A-1 性能优化 — 后处理并行化

`game_engine.py` 的 4 个后处理任务原本串行排队（单 worker），改为 `asyncio.gather` **并行执行**：

| 任务 | 职责 | 原 LLM 调用 |
|------|------|-------------|
| 历史摘要 | 压缩近期叙事 | 1 次 |
| NPC 印象更新 | 批量更新 NPC 对玩家印象 | 1-2 次 |
| 里程碑检测 | 识别关键事件触发 L3 摘要 | 0-1 次 |
| 情感评估 | 评估叙事情感强度 | 1 次 |

**关键改动**：4 个任务彼此独立（都只读 narrative），合并为 1 个 async wrapper 投递到后台队列。`_bg` 方法支持 async 函数的同步回退。**关键回合节省 5-15 秒**。

**调研后跳过的任务**：NPC 反应/演化循环并行化。每个 NPC 每天最多 1 次 LLM 反应（概率 15%），实际并行 LLM 调用通常 0-2 个；循环体内有共享状态访问（`_incr_react_count`、`_adjust_npc_favor`），并行化收益低、风险高。

#### 21.5.2 P4-A-2 成本控制 — BudgetGuard 全覆盖

**修复 1：daily_budget_usd 失效**

`config.json` 和 `config.json.example` 添加 `llm.pricing` 价格表：

```json
{
  "llm": {
    "pricing": {
      "deepseek-v4-flash": {"input": 0.00000027, "output": 0.00000110},
      "deepseek-v4-pro":   {"input": 0.00000275, "output": 0.00001100}
    }
  },
  "llm_budget": {
    "daily_budget_usd": 2.0  // 原 0.0（不限制）
  }
}
```

**修复 2：BudgetGuard 覆盖不全**

`router.py` 提取 `_budget_enter/_budget_success/_budget_failure` 3 个 helper，统一覆盖 5 个 LLM 方法：

| 方法 | 原状态 | 现状态 |
|------|--------|--------|
| `chat` | 已覆盖 | 已覆盖 |
| `chat_json` | 已覆盖 | 已覆盖 |
| `chat_structured` | ❌ 漏掉 | ✅ 已覆盖 |
| `achat` | ❌ 漏掉 | ✅ 已覆盖 |
| `achat_json` | ❌ 漏掉 | ✅ 已覆盖 |
| `achat_json_from_messages` | ❌ 漏掉 | ✅ 已覆盖 |
| `chat_json_from_messages` | ❌ 漏掉 | ✅ 已覆盖 |

**修复 3：fallback 清零失败计数 bug（原有 bug，测试挖出）**

- **问题**：所有 LLM 失败后走 `RuleBasedFallbackLLM`，fallback 成功后调用 `_budget_success` 清零了 `_consecutive_failures`，导致连续失败永远无法累积到 `circuit_failure_threshold`，**熔断保护形同虚设**
- **修复**：`router.py:303-307` 的 `_budget_success` 增加 `if llm is self.fallback: return` 判断，fallback 是规则兜底，不算 LLM 成功，不清零失败计数

**修复 4：缓存统计失真**

- `mimo_llm.py` `_get_cached` 命中/未命中时更新 `LLMCache.hit_count`/`miss_count`
- `chat_stream` 补 `stats.record_call`，流式调用的 token 统计不再丢失

**修复 5：配置无校验**

`config_schema.py` 新增 `LLMBudgetConfig` + `LLMConfig.pricing` 字段，类型错误可被 Pydantic 捕获。

#### 21.5.3 P4-A 真实环境验证结果

启动服务器 + DeepSeek API + E2E 脚本（16/16 PASS），对比修复前后：

| 指标 | 修复前 | 修复后 |
|------|--------|--------|
| `llm_cost_usd_today` | **0**（永远为 0） | **$0.0041** |
| `est_total_cost_usd` | 0 | **$0.0073** |
| `total_calls` | 仅 chat/chat_json 计数 | **41**（含 chat_structured） |
| `total_tokens` | 不完整 | **18,801**（prompt 7,644 + completion 11,157） |
| `router_chat_structured` | 不计数 | timing 记录中可见 |
| 熔断触发 | 永远不会 | 真正生效（5 次连续失败后熔断） |

### 21.6 v1.6 测试覆盖

| 测试套件 | 测试数 | 覆盖范围 |
|---------|-------|---------|
| test_resilience.py | 23 | retry/CircuitBreaker/timeout/safe_call |
| test_observability.py | 11 | request_id/结构化日志/health/timing |
| test_game_engine.py | +582 | __init__/plugin hooks/动作分类/undo_last_turn/init_llm 回退 |
| test_budget_guard_coverage.py | 17 | 5 个 LLM 方法的计数/熔断触发 |
| test_post_processing_parallel.py | 9 | asyncio.gather 并行/异常隔离/async 回退 |
| **总计** | **897** | **全部通过，0 failed** |

### 21.7 v1.6 新增模块清单

| 模块 | 行数 | 职责 |
|------|------|------|
| `modules/long_term_summary.py` | ~600 | 三级长期记忆摘要（L1/L2/L3） + MemoryAuditLog |
| `modules/emotional_manager.py` | ~450 | 8 维情感状态管理 + 情感评估器 |
| `core/resilience.py` | ~200 | retry_with_backoff/CircuitBreaker/call_with_timeout/safe_call |
| `core/request_id_middleware.py` | ~80 | request_id 中间件 + contextvars 传播 |
| `core/observability.py` | ~150 | RequestIdFilter + RotatingFileHandler 配置 |
| `modules/config_schema.py` | +LLMBudgetConfig | Pydantic 配置校验 |
| `benchmarks/e2e_verify.py` | ~400 | 16 项 E2E 验证脚本 |
| `app/config.json.example` | ~100 | 配置模板（无敏感信息） |
| `tests/test_resilience.py` | - | 23 项容错测试 |
| `tests/test_observability.py` | - | 11 项可观测性测试 |
| `tests/test_budget_guard_coverage.py` | - | 17 项 BudgetGuard 覆盖测试 |
| `tests/test_post_processing_parallel.py` | - | 9 项后处理并行测试 |

### 21.8 v1.6 修改要点

| 文件 | 修改 |
|------|------|
| `game_engine.py` | init_llm 配置回退；后处理 asyncio.gather 并行化；_bg 支持 async |
| `llm/router.py` | BudgetGuard 全覆盖 5 方法；fallback 不清零失败计数（Bug 修复） |
| `llm/mimo_llm.py` | LLMCache hit/miss 统计；chat_stream 补 stats.record_call |
| `config.json` | 新增 llm.pricing 价格表；daily_budget_usd 0.0 → 2.0 |
| `docker-compose.yml` | 新增 .secret_key 卷挂载 |
| `.dockerignore` | 保持 .secret_key 排除（不烤进镜像） |
| `routes/systems_routes.py` | +派系/记忆/情感相关 API 端点 |
| `routes/game_routes.py` | 请求模型默认值改空字符串触发 init_llm 回退 |
| 前端 | faction_panel.js / memory_panel.js / emotion_panel.js + sidebar 按钮 |

### 21.9 v1.6 关键经验

1. **BudgetGuard 原设计存在严重缺陷**：`daily_budget_usd=0` 默认值实际是不限制，且 fallback 成功清零失败计数导致熔断永远不触发。这是 v1.3 引入但 v1.6 才暴露的 bug，只有真实 LLM 调用才能测出成本累计
2. **WebSocket 在 accept 前关闭返回 HTTP 403**：FastAPI/Starlette 的设计，非 close code 4001。单元测试 mock 不准确，需 E2E 真实验证
3. **get_game_state() 不返回 npcs 字段**：必须通过 `GET /api/npcs` 单独获取（调用 `engine.get_npc_list()`）
4. **后处理并行化的边界**：只并行彼此独立、只读共享状态的任务；涉及写共享状态（如 NPC 反应循环修改其他 NPC 好感度）的任务并行化风险高、收益低
5. **密钥处理策略**：不烤进镜像（避免随镜像分发泄露），通过卷挂载运行时注入。容器内密钥必须与本机加密 config.json 使用的密钥一致

